<!-- SPDX-License-Identifier: AGPL-3.0-or-later -->

# OpenH2O 2.0 — UX Pattern Spec & Readiness Assessment

**Status (2026-06-22):** Design spec + live build tracker. Supersedes the
"convert every list-detail screen to master-detail" framing in
`2.0-UX-ROADMAP.md`. It is the rule set the remaining 2.0 work follows.

**Prod release policy (Brent, 2026-06-22):** NOTHING ships to prod until the whole
v2.0 is done. Prod stays frozen at v1.21 through every intermediate phase; all work
accumulates on `feature/master-detail-stations` + staging. The single prod release
happens only after Phase F (the aesthetics overhaul) lands, tagged `v2.0`. Do not
ship Phase B — or any phase — to prod on its own, even when verified. This
supersedes any "ship to prod together" wording below.

**Build progress (branch `feature/master-detail-stations`, staging only — prod
untouched at v1.21):**
- ✅ **Phase A — Zones** reworked to Bucket 3 (overview boundary-map + full-width
  list → per-zone detail page). Verified on staging.
- ✅ **Phase B — re-bucket the three live screens** (3 of 3 done; held on staging):
  - ✅ **Surface Diversions** → Bucket 3, point overview-map. Verified, tests pass.
  - ✅ **Water Rights** → Bucket 3, **no overview map** (non-spatial entity; its
    diversion points live on the detail page). Verified, tests pass.
  - ✅ **Recharge** → Bucket 3, **mixed-geometry overview-map** (polygon basins +
    point wells, one source with a `$type` filter). Verified, tests pass.
- ✅ **Phase C — Bucket-1 cleanup** (done; held on staging):
  - ✅ **Use Areas** → moved onto the shared workspace shell (was a hand-rolled
    copy). Bucket unchanged. Verified, tests pass.
  - ✅ **Water Accounts** → converted from a flat table to the master-detail
    workspace shell; full interactive detail in-pane + 'open full page' escape;
    runs map-less. Verified, tests pass.
- 🔄 **Phase D — Bucket-2 + remaining records** (Ledger done; bucketing decided):
  - ✅ **Use Ledger** → dense Bucket-2 table. It already had sortable sticky
    columns, filters, a zone facet, and paging; added the missing piece — a
    column-**totals** footer (net + credits/debits split) over the whole filtered
    set, pk-re-filtered so the zone M2M join can't double-count. Verified
    (totals reconcile to an independent SUM). Tests pass.
  - ✅ **Allocations** → **Bucket 2** (data table of plans w/ amounts). Mirrored the
    Ledger recipe — a totals footer summing `allocation_acre_feet` over the whole
    filtered set, not just the visible page. Allocations are unsigned positive
    volumes (no credit/debit polarity), so it's a single sum, not the ledger's
    split; zone is a ForeignKey (not M2M), so it aggregates directly with no
    pk-refilter. Verified: full = 326542.92 AF / 16 rows, one-period filter =
    166871.46 AF / 8 rows, both reconciling to an independent SUM. 47 tests pass.
  - ⬜ **Water Years** → no standalone screen exists today (no route). Nothing to
    bucket until/unless one is added.
  - 🔄 **Reports** → **Bucket 3** (few heavy items: each generates/downloads a
    filing — GEARS/CalWATRS CSV). Treat as an action-list overview → per-report
    detail. Assessed `reporting/`: it is NOT a stub — ~1,900 lines of working
    engine (generators, validators, filing lifecycle, OpenET prefill). The gap was
    purely the v2.0 skin; every screen still extended base.html.
    - ✅ **R1 (done; REVISED 2026-06-22)** — overview is a **Bucket-3 finder, NOT
      the master-detail workspace** — mirrors Water Rights. A report has no
      geometry, so the two-pane map shell only bought a hidden map slot and a
      permanently-empty right pane, with the "start a filing" actions and the
      history crammed into one narrow column. Rebuilt as a full-width page: a
      responsive action-card grid (GEARS / CalWATRS / shared-supply check) over a
      full-width history table whose rows link out to each submission's own detail
      page. report_detail keeps the shared _report_detail_pane (its HX path still
      returns the pane fragment for any future in-page caller; the standalone page
      is the rows' link target). report_list view dropped the ?selected= in-page
      pane preloading. Verified on staging (browser + computed styles); full suite
      879 passed.
      - **History:** R1 first shipped on the workspace master-detail shell
        (map-less, in-pane ?selected= detail). Reverted to the finder on
        2026-06-22 — the empty map slot made it feel awkward, exactly the
        "don't force task-flow screens into the master-detail shell" risk the
        Phase-D handoff had already flagged.
    - ✅ **R2 (done)** — the four full-page escapes (generate, OpenET prefill,
      CalWATRS worksheet, shared-supply check) were ALREADY on the v2.0 frame
      (breadcrumb-bar, page-stack, design tokens); they correctly stay full-page
      task flows (the master-detail shell would add an empty map slot for no gain).
      The only real inconsistency was shared_supply_check's breadcrumb root reading
      "Compliance" vs "Reporting" everywhere else — aligned. All four verified 200
      with a consistent root crumb; full suite 879 passed. **Reports bucket DONE.**
  - Deferred enhancement: Ledger **preset filter** chips ("This Period," "Active
    Use Areas"). The period auto-default + full filter bar already cover most of
    it; chips are polish, not a gap.
- ✅ **Phase E — Cross-cutting UX** (E1–E6) DONE 2026-06-22/23; E4 ongoing ride-along.
- ✅ **Phase F — Apple aesthetics overhaul** DONE. Token pass shipped 2026-06-23
  (b3f460e): OKLCH surface lift, gold reserved for state, loosened furniture,
  softer shadows, motion tokens, and the F2/F5/F6 a11y fixes — verified on the
  dashboard + by contrast math. See `docs/2.0-PHASE-F-DESIGN-PROPOSAL.md`.
- 🚀 **v2.0 SHIPPED to production 2026-06-23** (tag `v2.0` at c675434, deployed to
  openh2o.com via `make deploy`; footer stamps v2.0). Prod was frozen at v1.21.

**Why this exists:** The 2.0 work was scoped as "convert screens to master-detail."
That was wrong. Master-detail is one of *three* layout patterns, and which one a
screen should use depends on the user's task, the number of records, and how heavy
each record's detail is — not on a template applied uniformly. Several screens were
converted to master-detail that should not have been. This document defines the
deciding rule, assigns every screen to a pattern, lists the cross-cutting UX work
2.0 needs beyond layout, and fixes the sequence — ending with the purely-aesthetic
Apple overhaul as the final gate before the v2.0 tag.

---

## The core principle

**Layout follows the user's task, not the record count.** Count and detail-weight
are the two physical constraints that then decide which layout can actually serve
that task.

The three questions a screen answers map to the three layouts:

- "Which one, and what's its story?" → **master-detail**
- "How much, in total?" → **data table**
- "Show me this one thing, fully." → **overview + full detail page**

**Persona lens (decisive for this app):** users are non-technical staff at small,
under-resourced agencies who use the tool *infrequently* (seasonally, around
reporting deadlines). The governing principle for that audience — per USWDS, the
GOV.UK Design System, and Nielsen Norman Group — is **recognition over recall**:
the screen must re-teach itself every visit through plain language, visible options,
and forgiving findability. This is why the cross-cutting work (search, inline help)
matters as much as layout.

---

## The three buckets

### Bucket 1 — Master-detail workspace
**For:** many items, each with a medium-rich story; task is compare-and-browse with
frequent switching while staying in context.

**UI:** persistent searchable list (left) + selected item's detail (right), with a
map at the top of the detail when the item is spatial. Clicking a row swaps the
detail in place; the list never moves. A heavy detail offers an "open full page"
escape.

**Responsive collapse:** below ~720px the two columns can't coexist — collapse to
Bucket 3 (list, then a full detail view). This is a requirement, not an option.

**Screens:** Use Areas, Wells, Water Accounts.

### Bucket 2 — Dense data table
**For:** many items treated as *data*, thin per-row detail; task is filter, sort,
compare columns, and read totals.

**UI:** the whole screen is one table — filters/search across the top, sortable
columns, sticky header, and column **totals/subtotals** at the bottom. No detail
pane (there is nothing rich enough to warrant one); a row may link out to a related
record. Preset filters ("This Period," "Active Use Areas") aid infrequent users.

**Responsive collapse:** prioritize the key columns; allow horizontal scroll with a
frozen identifier column.

**Screens:** Use Ledger. (Allocations likely belongs here too — confirm in Phase D.)

### Bucket 3 — Overview, then full detail page
**For:** few items, each heavy; task is read-one-deeply, or triage then dive.

**UI:** two screens. **Overview** = a wide map on top showing all items at once,
with the list as full-width rows/cards below — no cramped side column. Clicking an
item (map point or list row) navigates to its **own full-width detail page**, where
the big map, charts, and sub-tables have room. Back returns to the overview. This is
the Monitoring Stations model.

**Responsive collapse:** naturally mobile-friendly — map stacks above list; detail
is already its own page.

**Non-spatial exception:** the overview map applies only when the item has
geometry of its own. **Water Rights** is Bucket 3 by count/weight but is a legal
entitlement with no geometry — so its overview is the full-width list alone, and
its spatial footprint (the diversion points it authorizes) appears on the detail
page, not the overview.

**Screens:** Zones, Recharge, Water Rights, Surface Diversions, Monitoring Stations.

---

## Per-screen assignment

Counts are from the Merced demo (one modest district); the design must hold at the
large end, so "scales?" matters more than the demo number.

| Screen | Demo count | Scales with district? | Bucket | Current state | Action |
|---|---|---|---|---|---|
| Use Areas (parcels) | 76 | Yes — thousands | **1** | Hand-rolled master-detail | ✅ Moved onto shared shell (Phase C). Done |
| Wells | 24 | Yes — hundreds+ | **1** | Master-detail (shell), shipped v1.16 | **Correct — keep** |
| Water Accounts | 11 | Yes — one per landowner | **1** | Simple table | ✅ Converted to master-detail (Phase C). Done |
| Use Ledger | 1,340 | Yes — unbounded | **2** | Sortable/filtered table | ✅ Bucket 2 — added totals footer (Phase D). Done |
| Surface Diversions | 9 | Rarely — one big diversion serves many fields | **3** | ✅ Bucket 3 (point overview-map) | **DONE** (Phase B) |
| Zones | 8 | No — bounded | **3** | ✅ Bucket 3 (boundary overview-map) | **DONE** (Phase A) |
| Recharge | 7 | No — bounded | **3** | ✅ Bucket 3 (mixed-geometry overview-map) | **DONE** (Phase B) |
| Water Rights | 6 | Mostly no | **3** | ✅ Bucket 3 (list only, no map — non-spatial) | **DONE** (Phase B) |
| Monitoring Stations | <~50 | No — bounded | **3** | Overview + full page | **Correct — the reference model** |
| Allocations | 16 | Yes | **2** | Sortable table | ✅ Bucket 2 — added totals footer (Phase D tail). Done |
| Water Years | — | — | n/a | No screen | No standalone screen exists; bucket if one is added |
| Reports | 11 | varies | **3** (finder) | Full-width finder (list only, no map — non-spatial) | ✅ Bucket 3 — R1 overview rebuilt as full-width finder (action-card grid + history table → detail page; revised 2026-06-22) + R2 (4 escapes already v2.0; breadcrumb aligned). Done |

**Net:** of the five master-detail screens already in production, **two are correct
(Use Areas*, Wells)** and **three are mis-classified and live (Surface Diversions,
Water Rights, Recharge)** — those three are the real remediation. (*Use Areas is
the right bucket but the wrong implementation.)

---

## Cross-cutting 2.0 upgrades (beyond layout)

Per-screen layout work does not touch these, and they are what make the app "easy to
use" for an infrequent, non-technical user.

**Tier 1 — foundational:**
- **Global search.** Today search is per-screen; there is no way to find "parcel
  MER-APN-014" without first knowing it lives under Use Areas. A top-bar search
  across parcels, wells, accounts, zones, and stations is the single highest-value
  addition in 2.0.
- **Bucket consistency.** Finish the system: re-bucket the three live screens, move
  Use Areas onto the shared shell, make the Ledger a real table. Consistency is a
  usability feature — it lets a returning user's skill transfer.
- **Responsive collapse** defined per bucket (above).

**Tier 2 — ease of use & flow:**
- **Inline contextual help.** A real glossary exists but is buried under Help. Add
  "?" info icons at the point of confusion and inline-link glossary terms.
- **Dashboard "what needs attention" glance.** The dashboard is tutorial-first
  (good for a fresh instance, thin for a returning admin). Add live exception tiles
  (stations down, periods unclosed, allocations over budget) above the tutorial.
- **Unify onboarding.** New users meet both a Setup Wizard and empty-state step
  lists; pick one spine.

**Tier 3 — compliance & polish:**
- **Accessibility pass** — skip-to-content link shipped (E3). The formal WCAG 2.1
  AA / Section 508 audit is DONE (2026-06-23) — see
  `docs/2.0-ACCESSIBILITY-AUDIT.md`. Result: Lighthouse 88/100; foundations solid
  (lang, landmarks, alt text, focus styles, zoom all pass). Open findings: 1
  Critical (master-detail rows not keyboard-operable — 2.1.1), 2 Serious
  (body-text contrast < AA; every page `<h1>` is the app name due to a
  `{% include %}` block-override bug), 3 Moderate, 2 Minor. **Remediation status
  (2026-06-23):** F1 (keyboard-operable master rows), F3 (real per-page `<h1>`)
  and F8 (modal heading order) are FIXED + verified. F2 (contrast), F5 (reduced
  motion) and F6 (non-text border contrast) are folded into Phase F (theme-level
  — see the Phase F section). F4 (colour-only links) and F7 (widget file-input
  label) remain as small standalone fixes.
- **Consistent visual grammar** — folded into the aesthetics pass below.

---

## Sequence (the order we build)

1. ✅ **Phase A — Lock the Bucket-3 pattern on Zones** (safely off-prod). Done +
   verified on staging; it is now the template.
2. 🔄 **Phase B — Re-bucket the three live screens** to Bucket 3 (2 of 3 done on
   branch/staging): Surface Diversions ✅, Water Rights ✅, **Recharge ⬜ next**.
   Then verify all three and ship to prod together.
3. **Phase C — Bucket-1 cleanup** (Use Areas onto the shared shell; Water Accounts
   to master-detail).
4. **Phase D — Bucket-2 + remaining records** (Ledger to a dense table; bucket
   Allocations, Water Years, Reports using the rule).
5. ✅ **Phase E — Cross-cutting UX** — E1–E6 DONE (E1/E3/E5 on 2026-06-22; E2/E6 on
   2026-06-23). E4 (inline "?" help) stays an ongoing ride-along sprinkled in as we
   touch screens; not a blocker. **Phase E is functionally complete.**
6. ✅ **Phase F — Apple aesthetics overhaul (FINAL GATE).** DONE 2026-06-23
   (b3f460e). Purely aesthetic; no structure moved. **v2.0 SHIPPED to prod the
   same day** (tag `v2.0`, openh2o.com). This was the last gate before the tag.

### Phase E sub-phases (scoped against the code, 2026-06-22)

Six cross-cutting items, ordered to lead with a fast self-contained win, then the
marquee feature, then polish. Each is its own sub-phase so we stay in the loop
between them. Build order ≠ spec tier order: a cheap high-value tile beats a big
feature for momentum. **Current state below is what the code actually has today —
three items are already part-built, so the real Phase E is smaller than it looks.**

- ✅ **E1 — Dashboard "what needs attention" strip (DONE 2026-06-22).** Brent
  picked the **compact strip** over prominent cards. A single-row exception bar at
  the top of the dashboard content (`_attention_strip.html`), each counter a
  clickable pill rendered only when non-zero; collapses to one "All clear" line
  when every count is zero. Counts: **periods to close** (`is_finalized=False` +
  `end_date` past → periods list), **stations down** (active stations classed
  `dead` by `datasync.freshness`, source-aware so it matches the Monitoring screen
  → station list), **accounts over budget** (active accounts whose consumptive use
  passed allocation this period → jumps to `#active-accounts`). Lives inside the
  HTMX-swapped content so the period-dependent over-budget count refreshes on
  period change. Verified on staging (1 / 3 / 5 live counts; browser + computed
  styles); +3 tests, full suite 882 passed. CSS in app.css (v51→v52).
- ✅ **E2 — Global search (DONE 2026-06-23).** Top-bar search box (signed-in only,
  hidden on phones) in `_header.html`, spanning all SIX primary entities — parcels,
  wells, surface diversions, monitoring stations, accounts, zones (Brent added
  surface diversions to the spec's five). A `global_search` view in `config/views.py`
  matches the identifier-or-name fields a user types (Q icontains), capped at 6 hits
  per group; the `_search_results.html` partial renders an HTMX live dropdown grouped
  by entity, each row linking to that record's detail page. Keyboard up/down/enter +
  outside-click/Escape dismissal; results container hides via `:empty`. Verified on
  staging (browser: all 6 groups, cap holds, click routes to detail, arrow-nav +
  Escape work; computed styles clean). +7 tests, full suite 892. app.css v56→v57.
- ✅ **E3 — Accessibility quick wins (DONE 2026-06-22).** Added a skip-to-content
  link as the first focusable element in `base.html` (off-screen until focused,
  `.skip-link` in app.css) targeting a new `id="main-content"` on `<main>`.
  Verified on staging (focus reveals it at top:16px, gold-on-dark, jumps to main).
  The formal WCAG 2.1 / 508 audit (contrast, focus order, keyboard traps) is still
  a separate effort for E6 / Phase F.
- 🔄 **E4 — Inline contextual help (STARTED 2026-06-22; ongoing, ride-along).**
  First application of the existing "?" pop-out (`partials/_explainer_popout.html`):
  dashboard budget-math columns (Net → water-balances; Allocation, Remaining →
  budgets-allocations help). Verified on staging. *Remaining:* keep sprinkling the
  "?" onto other points of confusion (ledger, allocations, surface) as we touch
  those screens — incremental, not a single push.
- ✅ **E5 — Responsive collapse audit (DONE 2026-06-22).** Audited all three
  buckets at 390px on staging. **Bucket 1** (master-detail) already collapses to
  one column below 900px. **Bucket 3** overview-map screens already stack (map is a
  full-width block above `#results` in a `page-stack`) — no work. **Bucket 2** was
  the real gap: the ledger and the dashboard account/zone tables were bare (no
  scroll wrapper), so on a phone their sideways scroll fell through to the whole
  page content (breadcrumbs + filters dragged sideways). Fix: a **phone-only**
  `.table-scroll-mobile` wrapper (overflow-x only under 767px) on the ledger card
  and the two dashboard tables — scoped to phones so desktop gains no overflow
  context (the ledger's sticky `<thead>` sticks to `.app-content` and would break
  otherwise). Verified: table's nearest scroller is now its own card, page no
  longer scrolls sideways. app.css v53→v54; +1 test.
  - *Known, out-of-scope:* on a phone the sidebar defaults **open** (covers content
    until the hamburger collapses it). Pre-existing, not introduced here; worth a
    look as a separate mobile-polish item, possibly with E6.
- ✅ **E6 — Unify onboarding (DONE 2026-06-23).** Brent picked the **Setup Wizard**
  as the spine. Scoping the code corrected the spec's "three competing surfaces"
  framing: the dashboard already defers to the wizard via `needs_setup`
  (`Boundary.exists()`), and its "Welcome" step-list covers post-setup *accounting*
  steps the wizard doesn't (periods → accounts → ledger), so it complements rather
  than duplicates — left as-is. The real gap was the four list screens' **dead-end
  empty states** ("No wells found", no action). Fixed: a shared
  `partials/_empty_onboarding.html` that each genuinely-empty list (Wells, Use Areas,
  Surface Diversions, Recharge) includes — on a fresh instance it points at the Setup
  Wizard; on a configured instance it offers that screen's own Add + Import; an empty
  *search* still shows the plain "no matches" text. Verified on staging (browser:
  both branches render on real CSS). +3 tests, full suite 895.
  - **Resting detail-pane fix (2026-06-23, after Brent flagged "empty states still
    look empty"):** the *real* eyesore wasn't the zero-records list state (rarely
    seen — every screen has demo data) but the **master-detail right pane before a
    row is picked** — a faint icon + one line in a tall empty column. Rebuilt the
    `_detail_empty`/`_account_detail_empty` panes (Use Areas, Wells, Accounts) as a
    composed orientation panel: disc icon, title, live `total_count`, and a "what
    opens here" preview list. Verified on staging (24 wells / 11 accounts / 76 use
    areas). Bucket-3 screens (Zones, Surface, Recharge) have no side pane, so their
    `_*_empty` partials are unused — edited for consistency but not rendered. app.css
    v57→v58.
  - *Known polish (Phase F):* in the narrow Bucket-1 left pane the "+ Add Well"
    button wraps to two lines; fine on full-width Bucket-3 screens.
  - *Deferred:* the 10-step Getting Started help page (`help/getting_started.html`)
    stays as a reference linked from "Get Help"; reconciling its wording with the
    wizard is optional polish, not a gap.

**Open decisions for Brent (before the gated sub-phases):** E2 — scope of global
search (which entities, how results route); E6 — which onboarding spine wins.

### Phase F — Apple aesthetics overhaul (the very end)

**This is the last step before 2.0 ships, and it is purely aesthetic** — type,
spacing, depth, color refinement, and motion, so the web app reads like the native
Mac app. It changes *no* structure, layout pattern, or behavior.

It comes last **by design**: structure stops moving first, so the visual polish is
applied once to settled screens and never redone as layouts change.

**Accessibility items folded into Phase F (from the 2026-06-23 audit, see
`docs/2.0-ACCESSIBILITY-AUDIT.md`).** These are theme-level and belong with the
colour/depth/motion pass so the visual work is done once on final tokens:
- **F2 — contrast (WCAG 1.4.3).** Route text-sized blue (`.data-table-link`,
  `.text-blue`, `.badge-blue`) to `--color-blue-bright` (#3DB4E0, 8.26:1) and
  nudge `--color-text-tertiary` lighter until it clears 4.5:1 on the `elevated`
  surface. Re-check `.text-supply` after.
- **F5 — reduced motion (WCAG 2.3.3).** Add a global
  `@media (prefers-reduced-motion: reduce)` override (loading bar + transitions).
- **F6 — non-text contrast (WCAG 1.4.11).** Raise the interactive-component
  border alpha (inputs/selects/buttons) to clear 3:1, independent of the
  decorative card-edge border.
(F1 keyboard rows, F3 per-page `<h1>`, F8 modal heading were fixed ahead of
Phase F on 2026-06-23 — they were not theme-level.)

**v2.0 does not ship until Phase F is complete.** The intermediate screen work ships
under incrementing point versions (v1.x); the `v2.0` tag is the finish line, applied
only after the aesthetics overhaul lands on top of finished structure.

---

## Research backing

The three-bucket logic matches established guidance: Nielsen Norman Group (data
tables; list entries; complex application design; progressive disclosure),
Microsoft's master-detail guidance (incl. the ~720px split-view threshold), and
enterprise data-table UX literature. The persona principles (task-based navigation,
plain language, recognition over recall, findability, accessibility) come from the
US Web Design System, the GOV.UK Design System, and NN/g's government/accessibility
guidance.
