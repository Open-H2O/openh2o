---
phase: 11-ui-quality-sweep
plan: 01
type: execute
---

<objective>
Polish navigation flow, visual consistency, responsive behavior, and data entry UX across all pages, with real Kaweah subbasin data visible in the system.

Purpose: Transform the MVP UI from functional-but-rough into demo-ready for GSA stakeholders. Every page should look intentional on desktop, tablet, and mobile.
Output: Responsive CSS, clean form widgets, consistent styling, and a polished landing page with live data counts.
</objective>

<execution_context>
~/.claude/get-shit-done/workflows/execute-phase.md
./summary.md
~/.claude/get-shit-done/references/checkpoints.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md

# Auto-selected based on dependency graph:
@.planning/phases/08-deploy-polish-handoff/08-02-SUMMARY.md
@.planning/phases/10-kaweah-demo-data/10-01-SUMMARY.md

# Key files:
@static/css/app.css
@static/css/tokens.css
@templates/base.html
@templates/partials/_sidebar.html
@templates/partials/_header.html
@templates/index.html
@accounting/forms.py
@reporting/forms.py

**Tech stack available:** Django 5, HTMX, Tailwind standalone, MapLibre GL JS, VanderDev design tokens (tokens.css)
**Established patterns:**
- Breadcrumb `{% block breadcrumbs %}` pattern on all pages
- `{% block page_description %}` for contextual help
- Toast notification via HX-Trigger header
- Semantic color classes: .text-supply/.text-usage/.text-surplus/.text-deficit
- Card-raised, stat-card, data-table CSS component patterns
- Form styling via inline `style` attributes on Django widget attrs (FORM_INPUT_STYLE / FORM_SELECT_STYLE constants)

**Constraining decisions:**
- [Phase 08-02]: Breadcrumbs, loading bar, toasts, semantic colors all established
- [Phase 10-01]: Real Kaweah data seeded; map has boundary/zone layers with auto-zoom
- No Node.js tooling; Tailwind standalone binary compiles to output.css
- app.css is hand-written CSS loaded separately from Tailwind output

**Issues being addressed:** None from ISSUES.md (all closed)
</context>

<tasks>

<task type="auto">
  <name>Task 1: Replace inline form styles with CSS classes</name>
  <files>accounting/forms.py, reporting/forms.py</files>
  <action>
Replace all `{"style": FORM_INPUT_STYLE}` with `{"class": "form-input"}` and all `{"style": FORM_SELECT_STYLE}` with `{"class": "form-select"}` in both forms.py files. Delete the FORM_INPUT_STYLE and FORM_SELECT_STYLE constants.

For textareas, use `{"class": "form-textarea"}`. For date inputs, use `{"class": "form-input", "type": "date"}`. For file inputs, use `{"class": "form-input"}`.

The CSS classes `.form-input`, `.form-select`, and `.form-textarea` already exist in app.css with identical styling. The inline style approach duplicates the CSS and prevents `:focus` ring styles from applying (the inline border overrides the CSS focus state).

Also add a `form-select` fix in app.css: the existing `.form-select` already has the chevron SVG background-image, but Django-rendered selects won't pick it up until they have the class. Verify the `:focus` box-shadow works after the switch.
  </action>
  <verify>
grep -c "FORM_INPUT_STYLE\|FORM_SELECT_STYLE" accounting/forms.py reporting/forms.py should return 0 for both files.
grep -c '"class": "form-input\|"class": "form-select\|"class": "form-textarea' accounting/forms.py reporting/forms.py should show the expected count.
  </verify>
  <done>All form widgets use CSS classes instead of inline styles. Focus rings (gold border + box-shadow) work on all form fields.</done>
</task>

<task type="auto">
  <name>Task 2: Add responsive CSS breakpoints</name>
  <files>static/css/app.css</files>
  <action>
Add responsive breakpoints to app.css for tablet (max-width: 1023px) and mobile (max-width: 767px). Target these specific layout classes:

**Tablet (max-width: 1023px):**
- `.page-grid-2col` → single column (`grid-template-columns: 1fr`)
- `.page-grid-2col-wide` → single column
- `.stat-grid-3col` → 2 columns

**Mobile (max-width: 767px):**
- `.stat-grid-3col` → single column
- `.form-grid-2col` → single column
- `.dashboard-grid` → `grid-template-columns: 1fr 1fr` (2 cols instead of auto-fill)
- `.app-content` → reduce padding to `var(--space-md)`
- `.breadcrumb-bar` → reduce padding to `var(--space-sm) var(--space-md)`
- `.app-header` → reduce padding to `var(--space-sm) var(--space-md)`
- Hide `.app-header-email` (email in header wastes space on mobile)

**Table responsiveness:**
- Wrap all `.data-table` in the existing `overflow-x: auto` pattern. Rather than modifying every template, add to the `.card-raised` that contains tables: if it contains a `.data-table`, let it scroll. Add a CSS rule: `.card-raised { overflow-x: auto; }` — this is safe because card-raised already has `overflow: hidden` behavior via border-radius.

Do NOT add overflow-x: auto to `.card-raised` globally (it would affect all cards). Instead, add a new utility class `.table-wrap` with `overflow-x: auto` and note in the action for Task 3 which templates to add it to. Actually, the parcels list already wraps its table card with `style="overflow-x: auto"`. Standardize this: add `.table-scroll` class to app.css with `overflow-x: auto` and `-webkit-overflow-scrolling: touch`.

Keep the existing mobile sidebar rules (lines 1120-1136) unchanged.
  </action>
  <verify>
The CSS should have @media blocks for 1023px and 767px breakpoints. Count responsive rules: grep -c "@media" static/css/app.css should show at least 3 (existing mobile + 2 new).
  </verify>
  <done>All grid layouts collapse gracefully on tablet and mobile. Tables scroll horizontally on narrow viewports. Header and breadcrumbs use tighter padding on mobile.</done>
</task>

<task type="auto">
  <name>Task 3: Visual consistency sweep across templates</name>
  <files>templates/index.html, templates/base.html, templates/parcels/partials/_list_results.html, templates/wells/list.html, templates/surface/water_rights_list.html, templates/recharge/list.html, templates/accounting/ledger_list.html, templates/datasync/station_list.html, templates/reporting/report_list.html, config/views.py</files>
  <action>
**3a. Landing page overhaul (index.html + config/views.py):**
The current landing page shows icon-only cards with no data counts and a hardcoded "v0.1.0" version. Update:
- In `config/views.py` (the root view), add context data: count of Parcels, Wells, WaterRights, RechargeSites, WaterAccounts, and MonitoringStations. Import models and use `.count()`.
- In `index.html`, display the actual count next to each card label (e.g., "Parcels" with "42" below it instead of just an SVG icon). Use the `.dashboard-card-value` class for the count and keep the icon smaller/above it.
- Replace hardcoded "v0.1.0" with dynamic version from Django settings or just remove the version — it's not maintained. Instead show a last-sync timestamp or just "System running".

**3b. Add favicon (base.html):**
Add a simple inline SVG favicon using a data URI in the `<head>`. Use a water droplet shape in California Gold (#E4A317). The SVG should be:
```
<link rel="icon" type="image/svg+xml" href="data:image/svg+xml,<svg xmlns='http://www.w3.org/2000/svg' viewBox='0 0 24 24'><path fill='%23E4A317' d='M12 2.69l5.66 5.66a8 8 0 1 1-11.31 0z'/></svg>">
```

**3c. Replace inline styles with CSS classes in templates:**
Scan templates for `style=` attributes that duplicate existing CSS classes. The main offenders:
- `style="overflow-x: auto;"` on table-wrapping cards → replace with class `table-scroll` (created in Task 2)
- `style="max-width: 640px;"` on form pages → add `.page-narrow` class to app.css (`max-width: 640px;`) and use it
- `style="max-width: 800px;"` on report detail → add `.page-medium` class (`max-width: 800px;`)
- `style="display: flex; flex-wrap: wrap; gap: var(--space-md); align-items: flex-end;"` on toolbar cards → add `.toolbar-row` class
- `style="flex: 1;"` spacer in _header.html → already has `margin-left: auto` on `.app-header-user`, remove the spacer div

Add these utility classes to app.css:
```css
.page-narrow { max-width: 640px; }
.page-medium { max-width: 800px; }
.toolbar-row { display: flex; flex-wrap: wrap; gap: var(--space-md); align-items: flex-end; }
.table-scroll { overflow-x: auto; -webkit-overflow-scrolling: touch; }
```

Apply `.table-scroll` to all list partial templates that wrap a `.data-table` in a `.card-raised`. The templates are:
- parcels/partials/_list_results.html (already has inline style — replace)
- wells/partials/_list_results.html (check if exists)
- accounting/partials/_ledger_list_results.html
- accounting/partials/_accounts_list_results.html
- accounting/partials/_allocations_list_results.html
- accounting/partials/_periods_list_results.html
- surface/partials/_list_results.html
- recharge/partials/_list_results.html
- datasync/partials/_station_list_results.html
- reporting/partials/_list_results.html

**3d. Replace emoji empty state icons with SVG:**
Templates use emoji characters (e.g., `&#x1F4CD;` pin) for empty states. These render inconsistently. Replace with small inline SVGs using the same stroke style as sidebar icons (stroke="currentColor", 24x24 viewBox). Use contextually appropriate icons:
- Parcels: grid icon (same as sidebar)
- Wells: target/bullseye icon (same as sidebar)
- Generic: a simple "no results" icon (circle with line)

Only fix the templates that have `.empty-state-icon` with emoji characters. Check each list partial.

**3e. Fix header spacer:**
In `_header.html`, remove the `<div style="flex: 1;"></div>` spacer. The `.app-header-user` already has `margin-left: auto` in the CSS. The spacer is redundant.
  </action>
  <verify>
grep -rn 'style="' templates/ | wc -l — count should be significantly lower than before (aim for <20 remaining, mostly in map scripts which are fine).
The landing page should show numeric counts for each entity type.
Favicon should appear in browser tab.
  </verify>
  <done>Landing page shows live entity counts. Favicon appears. Inline styles replaced with CSS classes across all list and form templates. Empty states use consistent SVG icons.</done>
</task>

<task type="checkpoint:human-verify" gate="blocking">
  <what-built>Responsive CSS, form widget cleanup, visual consistency sweep, landing page with data counts, and favicon across the entire OpenH2O UI</what-built>
  <auto-verify>
    1. SSH to Butler (ssh butler), cd /root/openh2o, git pull, docker compose up -d --build
    2. Wait for build to complete, verify containers healthy: docker compose ps
    3. Run migrations if any: docker compose exec web python manage.py migrate
    4. Collect static files: docker compose exec web python manage.py collectstatic --noinput
    5. curl -s -o /dev/null -w "%{http_code}" http://localhost:8000/ — expect 200 or 302
    6. Use chrome-devtools MCP to navigate to the deployed URL, take screenshots of:
       a. Landing page (check data counts visible)
       b. Parcels list (check table layout)
       c. Well detail page (check 2-col → 1-col at narrow width)
       d. Ledger create form (check form inputs have focus rings)
    7. Resize viewport to 768px width, take screenshot of parcels list (should still be usable)
    8. Resize to 375px width, take screenshot (mobile layout)
  </auto-verify>
  <human-verify>
    1. Landing page: data counts are visible and accurate, favicon shows in tab
    2. Desktop layout: cards, tables, and grids look balanced
    3. Tablet/mobile: grids collapse cleanly, tables scroll horizontally, no horizontal page overflow
    4. Forms: focus rings appear on input/select fields, date pickers work
  </human-verify>
  <resume-signal>Type "approved" to continue, or describe issues to fix</resume-signal>
</task>

</tasks>

<time_estimate>
**Shape:** 4 tasks (1 light, 1 medium, 1 medium, 1 checkpoint), main-context-only, standard.
**Token budget:**
  - Task 1 (Light): ~15K orchestrator tokens
  - Task 2 (Medium): ~25K orchestrator tokens
  - Task 3 (Medium): ~30K orchestrator tokens
  - Checkpoint overhead: ~10K
  - Total estimated: ~80K / 150K target (53% utilization)
**Historical matches:** Phase 11 injected telemetry n=17, median 24 min, p90 46 min.
**Wall-clock estimate:** 20-45 min.
**Session budget:** fits single session.
**Flags:** none.

Estimate based on injected session-metrics.jsonl telemetry brief (n=17 Phase 11 plans, median 24m, p90 46m).
</time_estimate>

<verification>
Before declaring phase complete:
- [ ] `docker compose exec web python manage.py check --deploy` passes (no new warnings)
- [ ] All form pages render with proper dark-mode styling and focus rings
- [ ] Landing page shows live entity counts from database
- [ ] Favicon appears in browser tab
- [ ] No horizontal overflow on any page at 375px viewport width
- [ ] No Python errors in `docker compose logs web`
- [ ] grep -rn "FORM_INPUT_STYLE\|FORM_SELECT_STYLE" returns 0 results in app code
</verification>

<success_criteria>

- All tasks completed
- All verification checks pass
- No errors or warnings introduced
- Forms use CSS classes, not inline styles
- All grid layouts are responsive at 3 breakpoints (desktop, tablet, mobile)
- Landing page shows live data from Kaweah seed
- Visual consistency: no emoji icons, no inline styles on layout elements
- Phase 11 complete
</success_criteria>

<output>
After completion, create `.planning/phases/11-ui-quality-sweep/11-01-SUMMARY.md`
</output>
