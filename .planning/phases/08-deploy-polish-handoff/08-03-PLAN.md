---
phase: 08-deploy-polish-handoff
plan: 03
type: execute
---

<objective>
Create demo fixtures, complete DEPLOY.md, add Makefile shortcuts, harden production settings, and write README.md.

Purpose: The platform is feature-complete but needs demo data for testing, a production-grade deployment guide, and documentation for open-source handoff. This plan makes the repo self-contained: anyone with Docker can clone, deploy, and see a working water accounting platform with realistic data.
Output: seed_demo_data command, complete DEPLOY.md, Makefile, production security, README.md, updated CLAUDE.md.
</objective>

<execution_context>
~/.claude/get-shit-done/workflows/execute-phase.md
~/.claude/get-shit-done/templates/summary.md
~/.claude/get-shit-done/references/checkpoints.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/08-deploy-polish-handoff/08-01-SUMMARY.md
@.planning/phases/08-deploy-polish-handoff/08-02-SUMMARY.md

@DEPLOY.md
@CLAUDE.md
@docker-compose.yml
@Caddyfile
@config/settings/base.py
@config/settings/production.py
@pyproject.toml

**Constraining decisions:**
- Butler server (192.168.0.114) is the deploy target
- Cloudflare Tunnel for public access at openh2o.com
- No Celery/Redis -- management commands + cron
- AUTH_USER_MODEL = 'core.User' (never change after migrations)
- 2-4GB RAM VPS target ($15/mo)
</context>

<tasks>

<task type="auto">
  <name>Task 1: Create demo fixture management command</name>
  <files>core/management/commands/seed_demo_data.py</files>
  <action>
Create a management command that generates realistic demo data for pilot testing.

The command should:
1. Check if demo data already exists (idempotent -- skip if GSA boundary "Demo Valley GSA" exists)
2. Create in order (respecting FK constraints):
   - 1 GSA boundary polygon (Demo Valley GSA, using a simple polygon around Merced County area)
   - 3 zones within the GSA (North Basin, Central Basin, South Basin)
   - 50 parcels with realistic APNs, owner names, and polygon geometries scattered across zones
   - 20 wells with well_registration_ids, types (agricultural/municipal/domestic), locations within parcels
   - 5 water meters distributed across wells
   - 3 water accounts (North Basin Agricultural, Central Basin Municipal, South Basin Mixed)
   - Assign parcels to accounts (15-20 parcels per account)
   - 2 reporting periods (WY 2024-2025 and WY 2025-2026, first finalized, second active)
   - 3 allocation plans (one per zone, with supply and usage type allocations)
   - ~200 ledger entries across both periods (mix of supply +AF and usage -AF)
   - 3 water rights with points of diversion
   - 2 recharge sites with 5 recharge events
   - 5 monitoring stations (2 CDEC, 1 USGS, 1 CIMIS, 1 NOAA) with mock sync data
   - 8 data sources configured (all with is_active=False for demo, mock_mode=True)

Use realistic California water district names and patterns:
- Owner names: mix of agricultural operations, municipalities, individual landowners
- APN format: XXX-XXX-XXX
- Well IDs: format matching DWR conventions
- Ledger volumes: realistic AF values (agricultural parcels: 2-5 AF/yr usage, 1-3 AF/yr supply)

Include --flush flag to delete existing demo data and recreate.
Print progress as it runs (e.g., "Created 50 parcels... Created 20 wells...").

Use Django's GEOSGeometry for spatial data. Keep geometries simple (rectangles for parcels, points for wells/stations).
  </action>
  <verify>docker compose exec web python manage.py seed_demo_data runs without errors. Dashboard shows realistic data. Map shows parcels and wells.</verify>
  <done>seed_demo_data creates complete demo dataset. --flush recreates from scratch. Dashboard, map, and list pages all populated.</done>
</task>

<task type="auto">
  <name>Task 2: Complete DEPLOY.md</name>
  <files>DEPLOY.md</files>
  <action>
Rewrite DEPLOY.md as the definitive AI-consumable deployment guide. Every step must be copy-pasteable with verification. An AI assistant reading this file should be able to deploy the platform on a fresh VPS without any other context.

Structure:
1. **Prerequisites** (keep existing, add: at least 2GB RAM, port 80/443 open)
2. **Quick Start** (5 commands to running platform with demo data)
3. **Clone and Configure**
   - git clone, cd, cp .env.example .env
   - Generate SECRET_KEY (python3 one-liner)
   - Set POSTGRES_PASSWORD
   - Set ALLOWED_HOSTS and CSRF_TRUSTED_ORIGINS for your domain
4. **Build and Start**
   - docker compose up -d --build
   - Wait for health check
   - Verify: docker compose ps output
5. **Initialize Database**
   - Run migrations
   - Create superuser
   - Seed reference data (seed_roles, seed_water_types, seed_data_sources)
   - Load demo data (seed_demo_data)
   - Collect static files
6. **Verify Deployment**
   - curl localhost returns 200
   - PostGIS loaded
   - Admin login works
   - Dashboard shows demo data
   - Map renders parcels and wells
   - Health endpoint returns green
7. **Production HTTPS** (domain setup)
   - Edit Caddyfile with your domain
   - Set DJANGO_SETTINGS_MODULE=config.settings.production
   - Restart
   - Verify HTTPS
8. **Configure Email** (optional, for password reset)
   - SMTP settings in .env
   - Test: password reset flow
9. **Configure Google OAuth** (optional)
   - Google Cloud Console: create project, OAuth consent screen, credentials
   - Set GOOGLE_CLIENT_ID and GOOGLE_CLIENT_SECRET in .env
   - Enable in admin: SiteConfig.allow_google_oauth = True
10. **Monitoring**
    - Health endpoint: /health/api/ (JSON, Uptime Kuma compatible)
    - Health dashboard: /health/
    - Cron: run_health_checks daily
    - Prune: prune_old_data monthly
11. **Backup**
    - pg_dump command for database
    - Media volume backup
    - Restore procedure
12. **Upgrade Path** (keep existing, expand)
13. **Troubleshooting** (keep existing, expand with common issues)

Each section has:
- Exact commands to run
- Expected output (what you should see)
- Failure mode (what to check if it doesn't work)
- Verification step

Write for an AI that has never seen this project. No assumptions about prior knowledge.
  </action>
  <verify>DEPLOY.md covers all 13 sections. Every command is copy-pasteable. Every section has verification.</verify>
  <done>DEPLOY.md is a complete, self-contained deployment guide. An AI can follow it to deploy on a fresh VPS.</done>
</task>

<task type="auto">
  <name>Task 3: Create Makefile with shortcuts</name>
  <files>Makefile</files>
  <action>
Create a Makefile with common development and operations shortcuts:

```makefile
.PHONY: up down build migrate seed demo health prune logs shell dbshell test css

up:
	docker compose up -d

down:
	docker compose down

build:
	docker compose up -d --build

migrate:
	docker compose exec web python manage.py migrate

seed:
	docker compose exec web python manage.py seed_roles
	docker compose exec web python manage.py seed_water_types
	docker compose exec web python manage.py seed_data_sources

demo:
	docker compose exec web python manage.py seed_demo_data

health:
	docker compose exec web python manage.py run_health_checks

prune:
	docker compose exec web python manage.py prune_old_data --confirm

logs:
	docker compose logs -f web

shell:
	docker compose exec web python manage.py shell

dbshell:
	docker compose exec db psql -U openh2o -d openh2o

css:
	docker compose exec web /app/scripts/build-css.sh

fresh: build migrate seed demo
	@echo "Fresh install complete. Visit http://localhost"
```
  </action>
  <verify>make up works. make health works. make --list (or grep PHONY Makefile) shows all targets.</verify>
  <done>Makefile has all shortcuts. make fresh does a complete setup from scratch.</done>
</task>

<task type="auto">
  <name>Task 4: Security hardening in production settings</name>
  <files>config/settings/production.py, .env.example</files>
  <action>
1. Update production.py with security settings:
   - SECURE_SSL_REDIRECT = True (Caddy handles HTTPS, but Django enforces)
   - SECURE_HSTS_SECONDS = 31536000 (1 year)
   - SECURE_HSTS_INCLUDE_SUBDOMAINS = True
   - SECURE_HSTS_PRELOAD = True
   - SESSION_COOKIE_SECURE = True
   - CSRF_COOKIE_SECURE = True
   - SECURE_PROXY_SSL_HEADER = ('HTTP_X_FORWARDED_PROTO', 'https')
   - X_FRAME_OPTIONS = 'DENY'
   - SECURE_CONTENT_TYPE_NOSNIFF = True
   - SECURE_BROWSER_XSS_FILTER = True (deprecated but harmless)

   Read existing production.py first to avoid duplicating settings that already exist.

2. Update .env.example with all required and optional environment variables:
   - SECRET_KEY= (required, with generation instructions in comment)
   - POSTGRES_PASSWORD= (required)
   - POSTGRES_USER=openh2o
   - POSTGRES_DB=openh2o
   - ALLOWED_HOSTS= (required for production)
   - CSRF_TRUSTED_ORIGINS= (required for production)
   - DJANGO_SETTINGS_MODULE=config.settings.local (default to local)
   - EMAIL_HOST= (optional, for password reset)
   - EMAIL_PORT=587
   - EMAIL_USE_TLS=True
   - EMAIL_HOST_USER= (optional)
   - EMAIL_HOST_PASSWORD= (optional)
   - DEFAULT_FROM_EMAIL=noreply@openh2o.com
   - GOOGLE_CLIENT_ID= (optional, for OAuth)
   - GOOGLE_CLIENT_SECRET= (optional, for OAuth)

Do NOT set SECURE_SSL_REDIRECT if there's already a check for whether HTTPS is configured (some setups run behind Cloudflare Tunnel without direct HTTPS).
Check if production.py reads SECURE_SSL_REDIRECT from env var -- if so, keep that pattern.
  </action>
  <verify>production.py has all SECURE_* settings. .env.example documents all variables with comments.</verify>
  <done>Production settings hardened. .env.example is a complete reference for all configuration.</done>
</task>

<task type="auto">
  <name>Task 5: Write README.md and update CLAUDE.md</name>
  <files>README.md, CLAUDE.md</files>
  <action>
1. Create README.md:
   - Project title and one-line description
   - "Why This Exists" section (3 sentences: the problem, the solution, the core value)
   - Screenshot placeholder (TODO: add screenshot after deployment)
   - Quick Start (5 commands: clone, configure, build, seed, visit)
   - Link to DEPLOY.md for full deployment guide
   - Features list (bullet points, organized by category)
   - Tech stack table
   - License (MIT or similar -- check if specified in PROJECT.md, if not use MIT)
   - Contributing section (brief, since this is primarily AI-deployed)

Keep README.md under 150 lines. Focus on "what is this" and "how do I run it" -- DEPLOY.md handles the details.

2. Update CLAUDE.md:
   - Add Phase 8 accomplishments to project context
   - Update Development Commands section with Makefile shortcuts
   - Add Testing section if test conventions were established
   - Update Project Structure with any new directories/files
   - Ensure Key Constraints still accurate
  </action>
  <verify>README.md renders correctly (no broken markdown). CLAUDE.md reflects current project state.</verify>
  <done>README.md provides quick project overview. CLAUDE.md is current and complete.</done>
</task>

<task type="checkpoint:human-verify" gate="blocking">
  <what-built>Complete platform with demo data, DEPLOY.md, security hardening, and documentation</what-built>
  <auto-verify>
    1. SSH to Butler, rebuild containers: docker compose up -d --build
    2. Run migrations: docker compose exec web python manage.py migrate
    3. Seed demo data: docker compose exec web python manage.py seed_demo_data
    4. Collect static: docker compose exec web python manage.py collectstatic --noinput
    5. Run health checks: docker compose exec web python manage.py run_health_checks --json
    6. curl http://192.168.0.114 -- verify 200
    7. curl http://192.168.0.114/health/api/ -- verify JSON with healthy status
  </auto-verify>
  <human-verify>
    1. Visit platform in browser -- demo data visible on dashboard
    2. Map shows parcels and wells
    3. List pages show data (not empty states)
    4. Breadcrumbs and page descriptions visible on all pages
    5. Loading bar appears during HTMX interactions
    6. Overall visual consistency (no broken styles, no unstyled elements)
  </human-verify>
  <resume-signal>Type "approved" to complete Phase 8, or describe issues to fix</resume-signal>
</task>

</tasks>

<verification>
Before declaring plan complete:
- [ ] seed_demo_data creates all demo entities without errors
- [ ] DEPLOY.md has all 13 sections with verification steps
- [ ] Makefile targets all work
- [ ] production.py has security settings
- [ ] .env.example documents all variables
- [ ] README.md is concise and accurate
- [ ] CLAUDE.md reflects current state
- [ ] Platform deployed on Butler with demo data
- [ ] Health checks pass
</verification>

<success_criteria>
- All tasks completed
- Demo data populates all views (dashboard, map, lists)
- DEPLOY.md is AI-consumable (every step copy-pasteable with verification)
- Security hardening applied for production
- README.md provides quick project overview
- Phase 8 (final phase) complete
</success_criteria>

<output>
After completion, create `.planning/phases/08-deploy-polish-handoff/08-03-SUMMARY.md`

Commit guidance:
- Task 1: `feat(08-03): create seed_demo_data management command`
- Task 2: `docs(08-03): complete DEPLOY.md deployment guide`
- Task 3: `chore(08-03): create Makefile with development shortcuts`
- Task 4: `feat(08-03): security hardening for production settings`
- Task 5: `docs(08-03): write README.md and update CLAUDE.md`
</output>
