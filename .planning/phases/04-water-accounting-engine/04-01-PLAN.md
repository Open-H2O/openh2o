---
phase: 04-water-accounting-engine
plan: 01
type: execute
---

<objective>
Stand up all accounting CRUD views: reporting periods, water accounts with parcel assignment, and allocation plans. Wire URL routing and sidebar navigation so the accounting section is navigable.

Purpose: Users need to create water years (reporting periods), set up accounts that group parcels, and configure allocation limits before they can start entering ledger data.
Output: 6 new list/detail/create views, updated sidebar, full URL routing for the accounting app.
</objective>

<execution_context>
~/.claude/get-shit-done/workflows/execute-phase.md
./summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md

# Prior phase context (same subsystem):
@.planning/phases/02-core-domain-models/02-03-SUMMARY.md
@.planning/phases/03-parcel-well-crud-maps/03-03-SUMMARY.md

# Existing models (already migrated, no schema changes needed):
@accounting/models.py
@parcels/models.py

# Established patterns to follow:
@parcels/views.py
@parcels/urls.py
@templates/parcels/list.html
@templates/parcels/detail.html
@templates/parcels/partials/_list_results.html
@templates/partials/_sidebar.html
@config/urls.py

**Tech stack available:** Django 5.x, HTMX (CDN), Tailwind standalone, MapLibre GL JS
**Established patterns:** HTMX partial response (HX-Request header), EDITABLE_FIELDS dict, card-raised styling, sidebar active-state, parse_qs for PATCH body, paginated list with search
**Constraining decisions:**
- [Phase 02-03]: WaterAccount, WaterAccountParcel, AllocationPlan, ReportingPeriod models already exist with migrations
- [Phase 03-03]: HTMX inline edit pattern established (GET form / PATCH save)
- [Phase 03-03]: EDITABLE_FIELDS dict drives field config (type/choices/validation in one config)
</context>

<tasks>

<task type="auto">
  <name>Task 1: URL routing and sidebar navigation for accounting</name>
  <files>accounting/urls.py, accounting/views.py, config/urls.py, templates/partials/_sidebar.html</files>
  <action>
  Create accounting/urls.py with URL patterns for:
  - reporting_periods/ (list), reporting_periods/create/ (create), reporting_periods/<pk>/ (detail), reporting_periods/<pk>/finalize/ (POST toggle)
  - accounts/ (list), accounts/create/ (create), accounts/<pk>/ (detail), accounts/<pk>/assign-parcel/ (POST), accounts/<pk>/remove-parcel/<wap_pk>/ (POST)
  - allocations/ (list), allocations/create/ (create), allocations/<pk>/edit/ (edit)

  Add `path("accounting/", include("accounting.urls"))` to config/urls.py.

  Update _sidebar.html: add an "Accounting" section header below existing nav links, with 4 links: Accounts, Periods, Allocations, Ledger (ledger URL will be added in 04-02). Use the same sidebar-link pattern with SVG icons. Use appropriate icons:
  - Accounts: wallet/briefcase icon
  - Periods: calendar icon
  - Allocations: pie-chart icon
  - Ledger: list/document icon (href="#" placeholder until 04-02)

  Active state: check for '/accounting/' in request.path, then refine by sub-path for each link.

  Create skeleton accounting/views.py with stub functions that return placeholder renders (will be filled in tasks 2 and 3).
  </action>
  <verify>docker compose exec web python -c "from django.urls import reverse; print(reverse('accounting:periods_list')); print(reverse('accounting:accounts_list')); print(reverse('accounting:allocations_list'))"</verify>
  <done>All accounting URLs resolve. Sidebar shows Accounting section with 4 links. No import errors.</done>
</task>

<task type="auto">
  <name>Task 2: Reporting period and allocation plan CRUD views</name>
  <files>accounting/views.py, templates/accounting/periods_list.html, templates/accounting/partials/_periods_list_results.html, templates/accounting/period_detail.html, templates/accounting/period_create.html, templates/accounting/allocations_list.html, templates/accounting/partials/_allocations_list_results.html, templates/accounting/allocation_create.html</files>
  <action>
  **Reporting Periods:**
  - periods_list: paginated list (25/page) with HTMX search by name. Columns: name, start_date, end_date, is_finalized (badge). Follow parcels_list pattern with HX-Request header check for partial response.
  - period_detail: show period info, finalized status, list of allocation plans for this period, count of ledger entries in this period. Include finalize/unfinalize button (POST to toggle endpoint).
  - period_create: simple form with name, start_date, end_date, notes. POST creates and redirects to detail. Use Django Form class in accounting/forms.py.
  - period_finalize: POST-only view. Toggles is_finalized, sets finalized_at and finalized_by. Redirects to detail.

  **Allocation Plans:**
  - allocations_list: paginated list with HTMX search. Columns: name, zone, water_type, reporting_period, allocation_acre_feet. Filter by reporting_period dropdown.
  - allocation_create: form with name, zone (dropdown), water_type (dropdown), reporting_period (dropdown), allocation_acre_feet, notes. POST creates and redirects to list.

  All templates use card-raised styling, VanderDev design tokens. Follow the exact same HTML/CSS patterns from parcels/list.html and parcels/detail.html.

  Create accounting/forms.py with ReportingPeriodForm and AllocationPlanForm (ModelForm subclasses). Style form inputs with dark-mode classes matching existing auth templates.
  </action>
  <verify>docker compose exec web python -c "from accounting.views import periods_list, period_detail, period_create, allocations_list, allocation_create; print('all views importable')"</verify>
  <done>Reporting period list/detail/create/finalize views render. Allocation list/create views render. HTMX search works on both lists. Finalize toggle works.</done>
</task>

<task type="auto">
  <name>Task 3: Water account CRUD with parcel assignment</name>
  <files>accounting/views.py, accounting/forms.py, templates/accounting/accounts_list.html, templates/accounting/partials/_accounts_list_results.html, templates/accounting/account_detail.html, templates/accounting/account_create.html, templates/accounting/partials/_parcel_assignment.html</files>
  <action>
  **Account List:**
  - accounts_list: paginated list (25/page) with HTMX search by account_number and name. Columns: account_number, name, status (badge), contact_name, parcel count (annotated). Filter by status dropdown. Same pattern as parcels_list.

  **Account Detail:**
  - account_detail: show account info (number, name, status, contact), list of assigned parcels (from WaterAccountParcel with reporting_period=current or null). Each parcel row shows parcel_number as link to parcel detail, added_date, and a remove button.
  - Include "Assign Parcel" section: HTMX-powered parcel search (type parcel number, see matches, click to assign). POST to assign-parcel endpoint creates WaterAccountParcel record. Use hx-post with hx-target to refresh the parcel list section.
  - assign_parcel view: POST accepts parcel_id, creates WaterAccountParcel. Returns updated parcel list partial.
  - remove_parcel view: POST deletes WaterAccountParcel. Returns updated parcel list partial.

  **Account Create:**
  - account_create: form with account_number, name, status, contact_name, contact_email, notes. WaterAccountForm in forms.py.

  **Parcel search for assignment:**
  - parcel_search_for_assignment: GET endpoint that accepts ?q= param, returns HTMX partial with matching parcels not already assigned to this account. Each result has an "Assign" button.

  All templates follow established card-raised, dark-mode patterns.
  </action>
  <verify>docker compose exec web python -c "from accounting.views import accounts_list, account_detail, account_create; print('all account views importable')"</verify>
  <done>Account list with search/filter renders. Account detail shows linked parcels. Assign/remove parcel workflow works via HTMX. Account create form works.</done>
</task>

</tasks>

<verification>
Before declaring plan complete:
- [ ] All accounting URLs resolve without errors
- [ ] `docker compose exec web python manage.py check` passes
- [ ] Sidebar shows Accounting section with 4 links
- [ ] Reporting period list renders with HTMX search
- [ ] Period create form submits and redirects
- [ ] Finalize/unfinalize toggles period status
- [ ] Account list renders with HTMX search
- [ ] Account detail shows assigned parcels
- [ ] Parcel assignment add/remove works
- [ ] Allocation plan list/create works
</verification>

<success_criteria>
- All tasks completed
- All verification checks pass
- No Django check warnings or errors
- HTMX search works on all list views without full page reload
- All views require login (@login_required)
- Templates follow VanderDev dark-mode design system
</success_criteria>

<output>
After completion, create `.planning/phases/04-water-accounting-engine/04-01-SUMMARY.md`
</output>
