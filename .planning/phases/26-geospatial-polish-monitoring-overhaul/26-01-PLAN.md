---
phase: 26-geospatial-polish-monitoring-overhaul
plan: 01
type: execute
---

<objective>
Unify all map colors under a single token system, fix entity-color bugs on detail pages, kill the slashed-zero font bleed, add breathing room to the main map container, improve the coordinate copy toast, and redesign the layer panel with grouped sections.

Purpose: Every map in the platform currently uses hardcoded hex colors that drift from the design tokens. Three detail pages show the wrong entity color (PODs and water rights show gold instead of teal; recharge sites show gold instead of purple). The station info card shows slashed zeros because Public Sans isn't inheriting. The layer panel is a flat unsorted list.
Output: All 13 maps use a centralized color system, entity colors are consistent between main map and detail pages, fonts render correctly everywhere, and the layer panel organizes layers into logical groups.
</objective>

<execution_context>
~/.claude/get-shit-done/workflows/execute-phase.md
./summary.md
~/.claude/get-shit-done/references/checkpoints.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/19.2-visual-overhaul-ux-refinement/19.2-01-SUMMARY.md
@.planning/phases/25-content-polish/25-01-SUMMARY.md

@static/css/tokens.css
@static/css/map-engine.css
@static/js/map-engine.js
@templates/geography/map.html
@templates/base.html
@tailwind.config.js

**Tech stack available:** Django 5.x, MapLibre GL JS (CDN), HTMX (CDN), Tailwind (standalone binary), VanderDev design tokens
**Established patterns:** tokens.css as CSS SSOT, cache-bust ?v=N on static assets, `git fetch origin && git reset --hard origin/main` for Butler deploy

**Constraining decisions:**
- [Phase 19.2-01]: Deep Pacific palette — gold accent is `#E4A317` (logo-derived amber-gold). tokens.css currently says `#D49A2B` which is wrong — it was the old pre-palette gold. Everything else uses `#E4A317`.
- [Phase 19.2-01]: `--radius-md: 8px` in tokens.css but Tailwind has `md: '10px'`. Pick one and align.
- [Phase 19.2-01]: Load tokens.css as separate `<link>` tag with own `?v=` for cache control.

**Issues being addressed:** None from ISSUES.md — ISSUE-005 (licensing) is unrelated.

**Key color audit (verified via grep):**
- tokens.css `--color-gold: #D49A2B` → should be `#E4A317`
- tokens.css `--color-gold-hover: #C48B24` → should match Tailwind's `#D4952A`
- Tailwind `borderRadius.md: '10px'` → should match tokens `--radius-md: 8px`
- Main map (map.html): Wells gold `#E4A317`, PODs teal `#4ECDC4`, Recharge purple `#7B68EE`, Stations red `#FF6B6B`, Boundary gold `#E4A317`, Parcels/Zones blue `#1B7FAF` — all hardcoded
- pod_detail.html: PODs shown as `#E4A317` gold (BUG — should be teal `#4ECDC4`)
- water_right_detail.html: PODs shown as `#E4A317` gold (BUG — should be teal `#4ECDC4`)
- recharge/site_detail.html: Recharge shown as `#E4A317` gold x3 (BUG — should be purple `#7B68EE`)
- map-engine.css: hardcodes `font-family:'Public Sans'` in 3 places (`.tb-btn`, `.maplibregl-popup-content`, `.maplibregl-ctrl-scale`, `#coord-toast`)
- map-engine.js: hardcodes `#E4A317` for measure tool marker and line

**DO NOT:**
- Don't change the MAP_CONFIG structure in map-engine.js — just extend it
- Don't rename "Allocation" — that's Phase 27
- Don't add Node.js dependencies
- Don't touch data models
</context>

<tasks>

<task type="auto">
  <name>Task 1: Reconcile design tokens and add entity color system</name>
  <files>static/css/tokens.css, tailwind.config.js, templates/base.html</files>
  <action>
  Fix the gold mismatch: update tokens.css `--color-gold` from `#D49A2B` to `#E4A317` and `--color-gold-hover` from `#C48B24` to `#D4952A` (matching Tailwind and all existing usage).

  Fix the border-radius mismatch: update tokens.css `--radius-md` from `8px` to `10px` (matching Tailwind's `borderRadius.md`). This is the established value used across the UI since Phase 19.2.

  Add entity-type color variables to tokens.css:
  ```
  --color-entity-gold: #E4A317;      /* Wells, Boundary outline */
  --color-entity-teal: #4ECDC4;      /* Points of Diversion */
  --color-entity-purple: #7B68EE;    /* Recharge Sites */
  --color-entity-red: #FF6B6B;       /* Monitoring Stations */
  --color-entity-blue: #1B7FAF;      /* Parcels, Zones */
  ```

  Add a small `<script>` block in base.html (before the closing `</body>` tag or in the head after CSS loads) that creates a global JS color object for MapLibre configs:
  ```javascript
  window.OH2O = { colors: {
    gold: '#E4A317', teal: '#4ECDC4', purple: '#7B68EE',
    red: '#FF6B6B', blue: '#1B7FAF', blueBright: '#3DB4E0',
    boundary: '#E4A317'
  }};
  ```
  This allows MapLibre paint properties (which can't use CSS var()) to reference a single source.

  Bump the cache version on the tokens.css `<link>` tag in base.html.
  </action>
  <verify>
  grep `--color-gold: #E4A317` tokens.css confirms gold is aligned.
  grep `--radius-md: 10px` tokens.css confirms radius is aligned.
  grep `OH2O` templates/base.html confirms JS color object exists.
  grep `--color-entity-` tokens.css shows all 5 entity colors defined.
  </verify>
  <done>tokens.css gold matches Tailwind and all usage sites. Entity color CSS vars defined. JS OH2O.colors object available globally. Cache version bumped.</done>
</task>

<task type="auto">
  <name>Task 2: Full hardcoded color sweep across all maps and detail templates</name>
  <files>templates/geography/map.html, templates/wells/detail.html, templates/surface/pod_detail.html, templates/surface/water_right_detail.html, templates/recharge/site_detail.html, templates/datasync/station_detail.html, templates/parcels/detail.html, templates/geography/zone_detail.html, templates/setup/confirm.html, templates/infrastructure/add.html, templates/geography/zone_create.html, static/js/map-engine.js, static/css/map-engine.css</files>
  <action>
  Replace all hardcoded hex colors in MapLibre JS configs with `OH2O.colors.*` references. Replace all hardcoded hex in CSS with `var(--color-entity-*)` references where applicable. This is a mechanical sweep — here's the mapping:

  **MapLibre paint/style in JS (`OH2O.colors.*`):**
  - `'#E4A317'` → `OH2O.colors.gold` (for wells, boundary) OR `OH2O.colors.teal` (for PODs) depending on entity
  - `'#4ECDC4'` → `OH2O.colors.teal` (PODs, SW tie lines)
  - `'#7B68EE'` → `OH2O.colors.purple` (recharge sites)
  - `'#FF6B6B'` → `OH2O.colors.red` (monitoring stations)
  - `'#1B7FAF'` → `OH2O.colors.blue` (parcels, zones, setup boundary)

  **CSS in map-engine.css (use `var(--color-entity-*)`):**
  - References to gold accent already use `var(--color-gold)` — verify these still work after the hex change.

  **BUG FIXES (entity color corrections):**
  - `pod_detail.html`: Change circle-color from gold to `OH2O.colors.teal`
  - `water_right_detail.html`: Change circle-color from gold to `OH2O.colors.teal`
  - `recharge/site_detail.html`: Change all 3 gold references (circle-color, fill-color, line-color) to `OH2O.colors.purple`

  **Also update:**
  - `map-engine.js` line 248: measure marker color `'#E4A317'` → `OH2O.colors.gold`
  - `map-engine.js` line 256: measure line color `'#E4A317'` → `OH2O.colors.gold`
  - `map.html` popup link style `color:#E4A317` → inline style stays but uses the same hex (fine since it's in a JS template literal, use `OH2O.colors.gold`)
  - `infrastructure/add.html`: All `#E4A317` → `OH2O.colors.gold`, `#1B7FAF` → `OH2O.colors.blue`
  - `zone_create.html`: Check for hardcoded colors, convert to OH2O.colors references
  - `setup/confirm.html`: `#1B7FAF` → `OH2O.colors.blue`

  **Swatches in MAP_CONFIG:** The `swatchColor` properties in MAP_CONFIG layers must also use `OH2O.colors.*`. These are used by map-engine.js to set inline styles on legend swatches.

  **DO NOT change** any layer IDs, source URLs, or MAP_CONFIG structure beyond the color values.
  </action>
  <verify>
  `grep -rn '#E4A317\|#4ECDC4\|#7B68EE\|#FF6B6B\|#1B7FAF' templates/ static/js/ --include='*.html' --include='*.js'` returns zero results (all hardcoded hex replaced).
  Spot-check: `grep 'OH2O.colors.teal' templates/surface/pod_detail.html` confirms POD uses teal.
  Spot-check: `grep 'OH2O.colors.purple' templates/recharge/site_detail.html` confirms recharge uses purple.
  </verify>
  <done>Zero hardcoded entity-color hex values remain in templates or JS. POD detail shows teal, recharge detail shows purple, water_right detail shows teal. All map colors reference OH2O.colors.*</done>
</task>

<task type="auto">
  <name>Task 3: Font sweep, map container breathing room, and coordinate toast upgrade</name>
  <files>static/css/map-engine.css, templates/geography/map.html, templates/datasync/station_detail.html, static/css/app.css</files>
  <action>
  **Font sweep:**
  - Remove all hardcoded `font-family:'Public Sans',sans-serif` from map-engine.css (`.tb-btn` line 25, `.maplibregl-popup-content` line 113, `.maplibregl-ctrl-scale` line 127, `#coord-toast` line 158). Replace with `font-family:var(--font-display)` so it inherits from tokens.css.
  - Run `grep -rn "font-family" templates/ static/ --include='*.html' --include='*.css' --include='*.js'` to find any other hardcoded font-family declarations. Every element should inherit from the body's `font-family: var(--font-display)` set in app.css, or use `var(--font-display)` / `var(--font-mono)` explicitly.
  - Check station_detail.html for the slashed-zero issue. The Station Information card likely has a numeric display (station ID, coordinates, elevation) that's picking up a fallback font. Ensure the card container or its parent inherits Public Sans. If needed, add `font-family: var(--font-display)` to the specific card or stat element.

  **Map container breathing room:**
  - In map.html, the `#map-wrapper` already has `margin: 4px 6px 4px 4px` and `border-radius: 6px`. Increase the margin to `8px 12px 8px 8px` to match the card spacing pattern used on other pages. Increase border-radius to `var(--radius-md, 10px)` for consistency with the card system.

  **Coordinate toast upgrade:**
  - In map-engine.css `#coord-toast` (line 154-160): Move from `bottom:50px` to `bottom:80px` for better visibility above the scale bar.
  - Increase `font-size` from `12px` to `13px`.
  - Add a brief checkmark animation: after copy, show "✓ Copied" instead of the current text, with a subtle scale pulse (`transform: scale(1.05)` for 200ms).
  - In map-engine.js `copyCoords()` function: update the toast text to include a checkmark "✓ Copied to clipboard" and add/remove a CSS class for the pulse.
  - Increase the earth emoji font-size in `#coords .coord-link` from `13px` to `15px` for better visibility.
  </action>
  <verify>
  `grep -n "font-family:'Public Sans'" static/css/map-engine.css` returns zero results.
  `grep -rn "font-family" static/css/ --include='*.css' | grep -v 'var(--font'` shows only the body rule in app.css (which already uses the var).
  `grep 'margin.*12px' templates/geography/map.html` confirms breathing room added.
  `grep 'Copied' static/js/map-engine.js` confirms toast text updated.
  </verify>
  <done>No hardcoded 'Public Sans' in map-engine.css. All font references use var(--font-display). Map container has 8-12px margins. Coordinate toast is larger, positioned higher, and shows "✓ Copied" with a checkmark.</done>
</task>

<task type="auto">
  <name>Task 4: Layer panel grouped redesign</name>
  <files>templates/geography/map.html, static/js/map-engine.js, static/css/map-engine.css</files>
  <action>
  Redesign the layer panel (#controls) from a flat list to grouped sections with collapsible headers.

  **Target grouping (from handoff research):**
  - Administrative: Boundary, GSA Zones
  - Land Use: Use Areas (parcels), Extraction Wells, Surface Diversions (PODs)
  - Infrastructure: Recharge Sites, Tie Lines (GW + SW)
  - Monitoring: Monitoring Stations

  **Implementation in MAP_CONFIG (map.html):**
  Add a `section` property to each layer config object in MAP_CONFIG.layers:
  ```javascript
  { id: 'boundary', section: 'Administrative', label: 'Boundary', ... },
  { id: 'zones-fill', section: 'Administrative', label: 'GSA Zones', ... },
  { id: 'parcels-fill', section: 'Land Use', label: 'Use Areas', ... },
  ...
  ```

  **Implementation in map-engine.js buildLayerPanel():**
  Modify `buildLayerPanel()` to group layers by `section`. For each section:
  1. Create a section header (`<div class="layer-section">`) with the section name
  2. Make the header clickable to collapse/expand the section
  3. Render the layer toggles within the section
  4. All sections start expanded (no hidden content on load)

  The section header should look like:
  ```html
  <div class="layer-section-header" onclick="this.parentElement.classList.toggle('collapsed')">
    <span class="section-chevron">▾</span> Administrative
  </div>
  ```

  **CSS additions to map-engine.css:**
  ```css
  .layer-section { margin-bottom: 8px; }
  .layer-section-header {
    font-size: 10px; font-weight: 700; letter-spacing: 0.8px;
    text-transform: uppercase; color: var(--color-text-tertiary);
    padding: 4px 0; cursor: pointer; display: flex; align-items: center; gap: 4px;
    user-select: none;
  }
  .layer-section-header:hover { color: var(--color-text-secondary); }
  .section-chevron { font-size: 8px; transition: transform 0.2s; }
  .layer-section.collapsed .section-chevron { transform: rotate(-90deg); }
  .layer-section.collapsed .layer-toggle { display: none; }
  ```

  **Backward compatibility:** If a layer in MAP_CONFIG has no `section` property, render it in an "Other" group. This ensures detail page mini-maps (which don't use sections) still work with map-engine.js.

  **Update the #controls h3 "Layers" heading** to use the gold accent color (already does via CSS). No change needed there.

  **DO NOT** change layer IDs, source URLs, paint properties, or the existing toggle checkbox behavior. The section grouping is additive.
  </action>
  <verify>
  Read map.html and confirm each MAP_CONFIG.layers entry has a `section` property.
  Read map-engine.js and confirm `buildLayerPanel()` creates section headers.
  Read map-engine.css and confirm `.layer-section-header` styles exist.
  `grep 'section:' templates/geography/map.html | wc -l` shows entries with section property.
  </verify>
  <done>Layer panel shows 4 collapsible sections (Administrative, Land Use, Infrastructure, Monitoring). Each section has a header with chevron toggle. All sections start expanded. Backward compatible with templates that don't use sections.</done>
</task>

<task type="checkpoint:human-verify" gate="blocking">
  <what-built>Complete visual polish: unified color tokens, entity-color bug fixes (POD→teal, recharge→purple), font sweep, map breathing room, coordinate toast upgrade, and grouped layer panel.</what-built>
  <auto-verify>
    1. SSH to Butler, pull latest, rebuild: `ssh butler "cd ~/openh2o && git fetch origin && git reset --hard origin/main && docker compose up -d --build"`
    2. Wait for build, then run collectstatic: `ssh butler "cd ~/openh2o && docker compose exec web python manage.py collectstatic --clear --noinput"`
    3. Verify app is running: `ssh butler "curl -s -o /dev/null -w '%{http_code}' http://localhost:80/"`
    4. Navigate to map page via chrome-devtools MCP, take screenshot
    5. Navigate to a POD detail page, verify circle color is teal (not gold) via computed styles
    6. Navigate to a recharge site detail page, verify fill color is purple (not gold)
    7. Navigate to station detail page, check font rendering (no slashed zeros)
    8. Verify no `font-family:'Public Sans'` hardcoded in map-engine.css via grep
    9. Verify no hardcoded `#E4A317` in templates via grep
  </auto-verify>
  <human-verify>
    1. Map page: layer panel shows 4 grouped sections with chevron toggles
    2. Map page: map has visible breathing room / margin around the edges
    3. Map page: click coordinates → toast appears prominently with "✓ Copied"
    4. Map page: toggle sections collapse/expand smoothly
    5. Station detail: no slashed zeros in numeric fields
    6. Overall: colors feel consistent between main map and detail pages
  </human-verify>
  <resume-signal>Type "approved" to continue to Plan 26-02 (Monitoring Overhaul), or describe issues to fix</resume-signal>
</task>

</tasks>

<time_estimate>
**Shape:** 5 tasks (1 light, 1 heavy, 2 medium, 1 checkpoint), main-context-only.
**Token budget:**
  - Task 1 (Light): ~15K orchestrator tokens
  - Task 2 (Heavy): ~40K orchestrator tokens (mechanical sweep across 13+ files)
  - Task 3 (Medium): ~20K orchestrator tokens
  - Task 4 (Medium): ~25K orchestrator tokens
  - Checkpoint overhead: ~10K
  - Total estimated: ~110K / 150K target (73% utilization)
**Historical matches:** No project-specific telemetry. Global baseline: n=1869, median 21 min/session.
**Wall-clock estimate:** 20-40 min (single session likely).
**Session budget:** Fits single session.
**Flags:** None.
</time_estimate>

<verification>
Before declaring plan complete:
- [ ] `grep -rn '#E4A317\|#4ECDC4\|#7B68EE\|#FF6B6B\|#1B7FAF' templates/ static/js/ --include='*.html' --include='*.js'` returns zero results
- [ ] `grep "font-family:'Public Sans'" static/css/map-engine.css` returns zero results
- [ ] tokens.css `--color-gold` is `#E4A317`
- [ ] POD detail page shows teal dot on mini-map
- [ ] Recharge detail page shows purple fill on mini-map
- [ ] Layer panel has 4 grouped sections
- [ ] Docker build succeeds on Butler
- [ ] All pages load without JS errors
</verification>

<success_criteria>

- All tasks completed
- All verification checks pass
- No hardcoded entity colors remain in templates or JS
- tokens.css and tailwind.config.js gold/radius values are aligned
- Font inheritance works everywhere (no slashed zeros)
- Layer panel is grouped and collapsible
- Deployed and verified on Butler
</success_criteria>

<output>
After completion, create `.planning/phases/26-geospatial-polish-monitoring-overhaul/26-01-SUMMARY.md`
</output>
