---
phase: 02-core-domain-models
plan: 01
type: execute
---

<objective>
Create the core and geography Django apps with all foundation models that other apps depend on.

Purpose: These 8 models (4 core + 4 geography) are the FK targets for every other app. They must exist first.
Output: core app expanded with User/Role/UserRole/SiteConfig, new geography app with Boundary/Zone/ZoneGroup/ParcelZone, django-allauth wired in settings.
</objective>

<execution_context>
~/.claude/get-shit-done/workflows/execute-phase.md
./summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/01-infrastructure-scaffold/01-01-SUMMARY.md

**Tech stack available:** Django 5.x, PostGIS 3.4, django-allauth[socialaccount], django-environ, django-htmx, whitenoise
**Established patterns:** Split settings (base/local/production), env-based config, AUTH_USER_MODEL='core.User'
**Constraining decisions:**
- Phase 1: Minimal User model stub already exists in core/models.py (AbstractUser subclass, no extra fields)
- Phase 1: AUTH_USER_MODEL='core.User' is set and must never change after migrations run

**Schema source:** ~/Documents/Work/Deliverables/open-water-platform-build-plan-2026-05-21.md (Section: Schema Overview)

**IMPORTANT: Do NOT run makemigrations or migrate in this plan.** Migrations are deferred to Plan 02-04 so all cross-app ForeignKeys resolve in a single pass.
</context>

<tasks>

<task type="auto">
  <name>Task 1: Expand core app models and wire allauth</name>
  <files>core/models.py, config/settings/base.py</files>
  <action>
  Expand core/models.py:
  - User: Add agency_admin BooleanField (default=False), phone CharField(max_length=20, blank=True), title CharField(max_length=100, blank=True). Keep AbstractUser inheritance.
  - Role: name CharField(max_length=50, unique=True), description TextField(blank=True). Seed values will be admin/manager/viewer (Phase 02-07).
  - UserRole: ForeignKey to User (CASCADE), ForeignKey to Role (CASCADE), unique_together on (user, role).
  - SiteConfig: agency_name CharField(max_length=200), timezone CharField(max_length=50, default='America/Los_Angeles'), native_srid IntegerField(default=4326), contact_email EmailField(blank=True), contact_phone CharField(max_length=20, blank=True), logo ImageField(upload_to='logos/', blank=True, null=True), allow_google_oauth BooleanField(default=False). Override save() to enforce singleton pattern (raise ValidationError if pk is None and SiteConfig.objects.exists()).

  In config/settings/base.py INSTALLED_APPS, add after existing entries:
  - "allauth"
  - "allauth.account"
  - "allauth.socialaccount"
  - "allauth.socialaccount.providers.google"
  - "geography"

  Add to MIDDLEWARE (after AuthenticationMiddleware):
  - "allauth.account.middleware.AccountMiddleware"

  Add allauth settings:
  - AUTHENTICATION_BACKENDS = ["django.contrib.auth.backends.ModelBackend", "allauth.account.auth_backends.AuthenticationBackend"]
  - ACCOUNT_EMAIL_REQUIRED = True
  - ACCOUNT_USERNAME_REQUIRED = False
  - ACCOUNT_AUTHENTICATION_METHOD = "email"
  - ACCOUNT_EMAIL_VERIFICATION = "optional"
  - LOGIN_REDIRECT_URL = "/"
  - LOGOUT_REDIRECT_URL = "/"
  - SOCIALACCOUNT_PROVIDERS = {} (empty dict, configured at runtime)

  Add to config/urls.py:
  - path("accounts/", include("allauth.urls"))
  </action>
  <verify>python -c "import core.models; print([m.__name__ for m in [core.models.User, core.models.Role, core.models.UserRole, core.models.SiteConfig]])" succeeds (syntax check only, no migrations yet)</verify>
  <done>core/models.py has User, Role, UserRole, SiteConfig. base.py has allauth wired. urls.py includes allauth URLs.</done>
</task>

<task type="auto">
  <name>Task 2: Create geography app with spatial models</name>
  <files>geography/models.py, geography/apps.py, geography/__init__.py, geography/admin.py</files>
  <action>
  Run: python manage.py startapp geography (or create files manually if running outside Docker).

  Create geography/models.py with:
  - Boundary: name CharField(max_length=200), description TextField(blank=True), geometry MultiPolygonField(srid=4326), area_sq_miles FloatField(null=True, blank=True), created_at DateTimeField(auto_now_add=True), updated_at DateTimeField(auto_now=True). Add __str__ returning name.
  - Zone: name CharField(max_length=200), boundary ForeignKey to Boundary (CASCADE), description TextField(blank=True), geometry MultiPolygonField(srid=4326), zone_type CharField(max_length=50, blank=True, choices for management_area/subbasin/custom), created_at/updated_at. Add __str__ returning name.
  - ZoneGroup: name CharField(max_length=200), description TextField(blank=True), zones ManyToManyField to Zone (blank=True). Add __str__ returning name.
  - ParcelZone: parcel ForeignKey to 'parcels.Parcel' (CASCADE, use string reference for cross-app FK), zone ForeignKey to Zone (CASCADE), unique_together on (parcel, zone).

  Set geography/apps.py with GeographyConfig, default_auto_field = "django.db.models.BigAutoField".

  Create empty geography/admin.py (admin registration is Plan 02-05).
  </action>
  <verify>python -c "import geography.models" succeeds (syntax check)</verify>
  <done>geography app has Boundary, Zone, ZoneGroup, ParcelZone models with GeoDjango spatial fields.</done>
</task>

</tasks>

<verification>
Before declaring plan complete:
- [ ] core/models.py defines User, Role, UserRole, SiteConfig (4 models)
- [ ] geography/models.py defines Boundary, Zone, ZoneGroup, ParcelZone (4 models)
- [ ] config/settings/base.py has allauth in INSTALLED_APPS and middleware
- [ ] config/urls.py includes allauth URLs
- [ ] No Python syntax errors in any modified file
- [ ] No migrations run (deferred to 02-04)
</verification>

<success_criteria>
- 8 models defined (4 core + 4 geography)
- allauth wired in settings, middleware, and URLs
- SiteConfig has singleton enforcement
- GeoDjango spatial fields used for Boundary and Zone
- All cross-app FKs use string references
</success_criteria>

<output>
After completion, create `.planning/phases/02-core-domain-models/02-01-SUMMARY.md`
</output>
