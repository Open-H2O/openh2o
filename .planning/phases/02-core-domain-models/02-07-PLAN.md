---
phase: 02-core-domain-models
plan: 07
type: execute
---

<objective>
Create seed data management commands and verify the complete Phase 2 build on Butler.

Purpose: Reference data (roles, water types, data sources) must be pre-populated so the platform is useful immediately after deployment. Final verification confirms all 48 models, auth, admin, and seed data work together.
Output: Three management commands that populate reference data, plus verified end-to-end functionality.
</objective>

<execution_context>
~/.claude/get-shit-done/workflows/execute-phase.md
./summary.md
~/.claude/get-shit-done/references/checkpoints.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/phases/02-core-domain-models/02-05-SUMMARY.md
@.planning/phases/02-core-domain-models/02-06-SUMMARY.md

**Seed data values from build plan:**
- Roles: admin, manager, viewer
- Water types: groundwater, surface_water, recycled, stormwater, imported, mixed
- Water right types: pre_1914_appropriative, post_1914_appropriative, riparian, pueblo, federal_reserved, statutory_small_domestic
- Well types: production, monitoring, injection, observation
- Data sources: cdec, usgs, openet, cimis, cnrfc, dwr_wdl, dwr_sgma, noaa (with URLs and auth types from build plan)
- Report templates: gears_by_well, gears_by_et, calwatrs_a1, calwatrs_a2, email_json

**Management command pattern:** Use get_or_create to make commands idempotent (safe to run multiple times).
</context>

<tasks>

<task type="auto">
  <name>Task 1: Create seed_roles management command</name>
  <files>core/management/__init__.py, core/management/commands/__init__.py, core/management/commands/seed_roles.py</files>
  <action>
  Create management command directory structure for core app.

  seed_roles.py:
  - BaseCommand subclass
  - Seed Role objects: admin (Full system access), manager (Manage parcels, wells, and accounts), viewer (Read-only access)
  - Use get_or_create for idempotency
  - Print created/existing status for each role
  - stdout: "Seeded 3 roles (X created, Y existing)"
  </action>
  <verify>docker compose exec web python manage.py seed_roles succeeds. Running twice produces same result (idempotent).</verify>
  <done>seed_roles command creates admin/manager/viewer roles idempotently.</done>
</task>

<task type="auto">
  <name>Task 2: Create seed_water_types and seed_data_sources commands</name>
  <files>accounting/management/__init__.py, accounting/management/commands/__init__.py, accounting/management/commands/seed_water_types.py, datasync/management/__init__.py, datasync/management/commands/__init__.py, datasync/management/commands/seed_data_sources.py, surface/management/__init__.py, surface/management/commands/__init__.py, surface/management/commands/seed_water_right_types.py, wells/management/__init__.py, wells/management/commands/__init__.py, wells/management/commands/seed_well_types.py, reporting/management/__init__.py, reporting/management/commands/__init__.py, reporting/management/commands/seed_report_templates.py</files>
  <action>
  Create management command directory structures for accounting, datasync, surface, wells, and reporting apps.

  seed_water_types.py (accounting app):
  - Seed WaterType objects with name and code:
    - Groundwater (GW), Surface Water (SW), Recycled Water (RW), Stormwater (ST), Imported Water (IW), Mixed (MX)
  - Use get_or_create, print status

  seed_water_right_types.py (surface app):
  - Seed WaterRightType objects with name, code, description:
    - Pre-1914 Appropriative (PRE14, Rights established before the 1914 Water Commission Act)
    - Post-1914 Appropriative (POST14, Permitted rights under SWRCB jurisdiction)
    - Riparian (RIP, Rights tied to land adjacent to a natural watercourse)
    - Pueblo (PUE, Municipal rights from Spanish/Mexican law)
    - Federal Reserved (FED, Rights reserved by federal land designation)
    - Statutory Small Domestic (SSD, Registrations for small domestic use under Water Code 1228)
  - Use get_or_create, print status

  seed_well_types.py (wells app):
  - Seed WellType objects: Production, Monitoring, Injection, Observation
  - Use get_or_create, print status

  seed_data_sources.py (datasync app):
  - Seed DataSource objects with name, code, url, auth_type, sync_interval_hours, description:
    - CDEC (cdec, https://cdec.water.ca.gov, none, 24, California Data Exchange Center)
    - USGS NWIS (usgs, https://waterservices.usgs.gov, api_key, 24, USGS National Water Information System)
    - OpenET (openet, https://openet.dri.edu, api_key, 720, Satellite evapotranspiration data) (30-day sync for monthly data)
    - CIMIS (cimis, https://et.water.ca.gov, api_key, 24, California Irrigation Management Information System)
    - CNRFC (cnrfc, https://www.cnrfc.noaa.gov, none, 24, California Nevada River Forecast Center)
    - DWR Water Data Library (dwr_wdl, https://wdl.water.ca.gov, none, 168, DWR groundwater level data) (weekly)
    - DWR SGMA Portal (dwr_sgma, https://sgma.water.ca.gov, none, 720, DWR SGMA monitoring data) (monthly)
    - NOAA NCEI (noaa, https://www.ncei.noaa.gov, token, 24, NOAA National Centers for Environmental Information)
  - Use get_or_create, print status

  seed_report_templates.py (reporting app):
  - Seed ReportTemplate objects:
    - GEARS by Well (gears_by_well, Per-well monthly extraction volumes)
    - GEARS by ET (gears_by_et, Per-parcel ET-based extraction estimates)
    - CalWATRS A1 (calwatrs_a1, Diversion to direct use monthly volumes)
    - CalWATRS A2 (calwatrs_a2, Diversion to storage monthly volumes)
    - Email+JSON (email_json, Structured email reporting via Power Automate)
  - Use get_or_create, print status

  Create a convenience command: core/management/commands/seed_data.py that calls all five seed commands in order.
  </action>
  <verify>docker compose exec web python manage.py seed_data succeeds. All reference data populated. Running twice is idempotent.</verify>
  <done>5 seed commands created (seed_roles, seed_water_types, seed_water_right_types, seed_well_types, seed_data_sources, seed_report_templates) plus seed_data umbrella command. All idempotent.</done>
</task>

<task type="checkpoint:human-verify" gate="blocking">
  <what-built>Complete Phase 2: 48 Django models across 11 apps, admin for all models, django-allauth auth flow with styled templates, Google OAuth toggle, 6 seed data commands</what-built>
  <how-to-verify>
    1. SSH to Butler: ssh butler
    2. Rebuild and verify: cd ~/openh2o && docker compose up -d --build
    3. Run migrations: docker compose exec web python manage.py migrate
    4. Seed data: docker compose exec web python manage.py seed_data
    5. Create superuser: docker compose exec web python manage.py createsuperuser
    6. Visit admin: http://192.168.0.114/admin/ — verify all 48 models appear
    7. Visit login: http://192.168.0.114/accounts/login/ — verify styled login page
    8. Register a new account — verify registration works
    9. Visit index while logged in — verify authenticated content
    10. Log out — verify anonymous content
    11. Check admin for seeded data: Roles (3), Water Types (6), Data Sources (8), Water Right Types (6), Well Types (4), Report Templates (5)
  </how-to-verify>
  <resume-signal>Type "approved" to complete Phase 2, or describe issues to fix</resume-signal>
</task>

</tasks>

<verification>
Before declaring plan complete:
- [ ] seed_roles creates 3 roles
- [ ] seed_water_types creates 6 water types
- [ ] seed_water_right_types creates 6 water right types
- [ ] seed_well_types creates 4 well types
- [ ] seed_data_sources creates 8 data sources
- [ ] seed_report_templates creates 5 report templates
- [ ] seed_data runs all commands in sequence
- [ ] All commands are idempotent
- [ ] Human verification of full stack on Butler
</verification>

<success_criteria>
- All seed commands work and are idempotent
- seed_data umbrella command runs all seeds
- Full stack verified on Butler: models, admin, auth, seed data
- Phase 2 complete: 48 models, admin, auth, seed data all working
</success_criteria>

<output>
After completion, create `.planning/phases/02-core-domain-models/02-07-SUMMARY.md`

This is the final plan in Phase 2. Summary should include:
- Total model count verified
- Auth flow status
- Admin coverage
- Seed data status
- "Phase 2 complete, ready for Phase 3: Parcel and Well CRUD with Maps"
</output>
