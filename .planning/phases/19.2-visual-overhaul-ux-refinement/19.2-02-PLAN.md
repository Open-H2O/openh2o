---
phase: 19.2-visual-overhaul-ux-refinement
plan: 02
type: execute
---

<objective>
Complete the visual overhaul phase: reorganize sidebar navigation by domain, redesign the reports page with hero cards, and scope station views to the district boundary.

Purpose: Transform navigation from a flat list into domain-grouped sections that match how a water district manager thinks about their work. Make reports more discoverable with dedicated cards per report type. Filter stations to only those within the operating boundary.
Output: Restructured sidebar, redesigned reports page, boundary-scoped station views.
</objective>

<execution_context>
~/.claude/get-shit-done/workflows/execute-phase.md
./summary.md
~/.claude/get-shit-done/references/checkpoints.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/19.2-visual-overhaul-ux-refinement/19.2-01-SUMMARY.md
@.planning/phases/19.2-visual-overhaul-ux-refinement/19.2-CONTEXT.md

# Key files:
@templates/partials/_sidebar.html
@templates/reporting/report_list.html
@templates/reporting/partials/_tab_content.html
@reporting/views.py
@datasync/views.py
@datasync/models.py
@geography/models.py
@static/css/app.css
@templates/base.html

**Tech stack available:** Django 5.x, HTMX, MapLibre GL JS, Tailwind standalone, PostGIS
**Established patterns:** VanderDev design tokens (tokens.css), Deep Pacific palette, card-raised class, form-input/form-select classes, sidebar-link/sidebar-section-label classes, HTMX partial rendering
**Constraining decisions:**
- Phase 19.2-01: Deep Pacific palette (amber-gold accent, logo-extracted blue), single-source shadows, tightened radii, Public Sans 300-800
- Phase 19.2-01: "Surface Water" rename already applied
- Phase 19.2-01: Reports already have GEARS/CalWATRS tabs with HTMX switching
- Boundary access pattern: `Boundary.objects.first()` (used in discover_stations and setup wizard)
</context>

<tasks>

<task type="auto">
  <name>Task 1: Reorganize sidebar into domain-grouped sections</name>
  <files>templates/partials/_sidebar.html</files>
  <action>
  Restructure the sidebar sections from flat "Data / Reporting / System / Setup / Help" to domain-organized groupings that match how a water district manager thinks:

  1. **No section label (top):** Dashboard, Map
  2. **Water Resources:** Parcels, Wells, Surface Water, Recharge, Infrastructure
  3. **Monitoring:** Stations
  4. **Accounting:** Ledger, Accounts, Periods, Allocations
  5. **Compliance:** Reports
  6. **Admin:** Health, Setup Wizard
  7. **Help:** Getting Started, Glossary, About

  Keep all existing sidebar-link classes, SVG icons, URL patterns, and active-state detection unchanged. Only change the section groupings and section labels.

  Do NOT change any URLs, Python code, or view logic. This is purely a template reorganization.

  Do NOT rename any link text (Surface Water rename was already done in 19.2-01).
  </action>
  <verify>
  - grep for each URL name to confirm all sidebar links still present
  - Count sidebar links: should be 16 (Dashboard, Map, Parcels, Wells, Surface Water, Recharge, Infrastructure, Stations, Ledger, Accounts, Periods, Allocations, Reports, Health, Setup Wizard, Getting Started, Glossary, About)
  - Section labels match: Water Resources, Monitoring, Accounting, Compliance, Admin, Help
  </verify>
  <done>Sidebar shows 7 logical groups. All 16 links present with correct URLs. Active states work. Section labels are plain-English domain names.</done>
</task>

<task type="auto">
  <name>Task 2: Redesign reports page with hero cards and history table</name>
  <files>templates/reporting/report_list.html, templates/reporting/partials/_tab_content.html, reporting/views.py, static/css/app.css</files>
  <action>
  Replace the current tab-switcher interface with a two-card layout that makes both report types immediately visible and actionable:

  **Layout structure:**
  1. Two side-by-side hero cards at the top (CSS grid, 1fr 1fr):
     - **GEARS card:** Title "GEARS", description "Groundwater Extraction Annual Report. Per-well extraction volumes for SGMA compliance reporting to the Department of Water Resources.", count of existing GEARS reports, "Generate Report" button linking to report_generate with ?type=gears
     - **CalWATRS card:** Title "CalWATRS", description "California Water Transfer Reporting System. Surface water diversion records for the State Water Resources Control Board.", count of existing CalWATRS reports, "Generate Report" button linking to report_generate with ?type=calwatrs
     - Cards use card-raised class, amber-gold top border accent (like stat cards)
  2. Below the cards: "Report History" section with a combined table of all past reports (both types), showing type badge (GEARS/CalWATRS), period, status, generated date. Keep existing search + status filter.

  **View changes (reporting/views.py):**
  - Add gears_count and calwatrs_count to report_list context
  - Remove tab-based queryset filtering from the full page render (history table shows all reports)
  - Keep HTMX partial for filtered results (search/status filter still works)

  **CSS (app.css):**
  - Add .report-type-badge class for inline type labels in the history table
  - Remove .report-tabs and .report-tab styles (no longer needed)
  - Keep other reporting CSS intact

  Do NOT change report_generate, report_detail, or report_download views. Only modify report_list and its templates.
  </action>
  <verify>
  - Load /reporting/ page: two cards visible side by side
  - Each card shows correct description, count, and generate link
  - History table below shows all reports with type badge
  - Search and status filter still work via HTMX
  - Generate Report buttons link to correct URLs
  </verify>
  <done>Reports page shows two hero cards (GEARS + CalWATRS) with descriptions and generate buttons. History table below shows all past reports with type badges. No tabs. Search/filter still functional.</done>
</task>

<task type="auto">
  <name>Task 3: Scope station views to district boundary</name>
  <files>datasync/views.py, templates/datasync/station_list.html, templates/datasync/monitoring_dashboard.html</files>
  <action>
  Add a PostGIS spatial filter to both station_list and monitoring_dashboard views so they only show stations within the district boundary:

  **View changes (datasync/views.py):**
  1. Import Boundary from geography.models
  2. In both station_list and monitoring_dashboard:
     - Get boundary: `boundary = Boundary.objects.first()`
     - If boundary exists, add `.filter(location__within=boundary.geometry)` to the MonitoredStation queryset
     - Pass `boundary_name = boundary.name if boundary else None` to template context
  3. Apply the boundary filter BEFORE the search/source/active filters (so text search only searches within-boundary stations)
  4. Also apply to the `all_active` count and `source_status_list` queries in station_list (so summary stats reflect boundary-scoped data)

  **Template changes:**
  - In station_list.html: Add a small indicator below the breadcrumb description showing "Showing stations within [boundary name]" when boundary is present. Use text-secondary text-sm styling.
  - In monitoring_dashboard.html: Same boundary indicator

  **Fallback:** If no Boundary exists (fresh install with no boundary set up yet), show all stations with no indicator. This preserves backward compatibility.

  Do NOT modify station_detail, station_add, station_toggle, or any management commands. Only scope the list/dashboard views.
  </action>
  <verify>
  - With a boundary in the database: station count matches stations whose location is within the boundary geometry
  - Without a boundary: all stations shown (same as current behavior)
  - Boundary name indicator visible on both station_list and monitoring_dashboard pages
  - Summary stat cards (Active, Fresh, Stale) reflect boundary-scoped counts
  </verify>
  <done>Station list and monitoring dashboard filter to boundary-scoped stations. Indicator shows boundary name. Fallback to all stations when no boundary exists. Stats reflect scoped data.</done>
</task>

<task type="checkpoint:human-verify" gate="blocking">
  <what-built>Sidebar domain grouping, reports hero cards, and boundary-scoped stations</what-built>
  <auto-verify>
    1. SSH to Butler, pull latest, rebuild containers: `ssh butler "cd ~/GitHub/openh2o && git fetch origin && git reset --hard origin/main && docker compose up -d --build && docker compose exec web python manage.py collectstatic --clear --noinput"`
    2. Bump cache version in base.html if needed
    3. curl the reports page, confirm 200 and check for "GEARS" and "CalWATRS" card content
    4. curl the stations page, confirm 200 and check for boundary indicator text
    5. Use chrome-devtools to navigate to each page and take screenshots
    6. Verify sidebar section labels render correctly with computed styles
  </auto-verify>
  <human-verify>
    1. Sidebar groupings feel intuitive: Water Resources, Monitoring, Accounting, Compliance, Admin, Help
    2. Reports page: two cards are visually balanced, descriptions readable, generate buttons prominent
    3. Station page: boundary indicator is subtle but visible
    4. No broken layouts, missing icons, or visual regressions from 19.2-01 work
  </human-verify>
  <resume-signal>Type "approved" to continue, or describe issues to fix</resume-signal>
</task>

</tasks>

<time_estimate>
**Shape:** 4 tasks (3 Light + 1 checkpoint), main-context-only, standard.
**Token budget:**
  - Task 1 (Light): ~10K orchestrator tokens
  - Task 2 (Light-Medium): ~15K orchestrator tokens
  - Task 3 (Light): ~10K orchestrator tokens
  - Checkpoint overhead: ~10K
  - Total estimated: ~45K / 150K target (30% utilization)
**Historical matches:** Phase 19 median 20 min per session (n=33 sessions).
**Wall-clock estimate:** 20-30 min.
**Session budget:** Fits single session comfortably.
**Flags:** None.

Source: GSD telemetry brief (session-metrics.jsonl), Phase 19 baseline.
</time_estimate>

<verification>
Before declaring phase complete:
- [ ] All 16 sidebar links present and functional
- [ ] Sidebar section labels: Water Resources, Monitoring, Accounting, Compliance, Admin, Help
- [ ] Reports page shows two hero cards with correct content
- [ ] Reports history table shows all report types with badges
- [ ] Station list filtered to boundary when boundary exists
- [ ] Station stats (Active, Fresh, Stale) reflect boundary-scoped data
- [ ] No Python errors (docker compose logs)
- [ ] No visual regressions from 19.2-01
</verification>

<success_criteria>

- All tasks completed
- All verification checks pass
- No errors or warnings introduced
- Sidebar navigation organized by domain
- Reports page uses hero cards instead of tabs
- Station views scoped to district boundary
- Phase 19.2 complete
</success_criteria>

<output>
After completion, create `.planning/phases/19.2-visual-overhaul-ux-refinement/19.2-02-SUMMARY.md`:

# Phase 19.2 Plan 02: Sidebar Reorg, Reports Redesign, Station AOI Scoping Summary

**[Substantive one-liner]**

## Accomplishments

- [Key outcomes]

## Files Created/Modified

- [File list with descriptions]

## Decisions Made

[Key decisions and rationale, or "None"]

## Issues Encountered

[Problems and resolutions, or "None"]

## Next Step

Phase 19.2 complete, ready for Phase 20 (AI Operator Guide).
</output>
