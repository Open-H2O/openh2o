---
phase: 26.1-monitoring-completion
plan: 01
type: execute
domain: django-datasync
---

<objective>
Wire remaining data adapters for live telemetry sync, fix chart parameter labels and Y-axis units, and audit all monitoring pages for missing units. When complete, every monitoring station shows human-readable labels, proper units, and live data from its source API.

Purpose: The monitoring tab currently works for CDEC only. Four other adapter sources (USGS, DWR Water Data Library, DWR SGMA Portal, CIMIS) have code but have never run live. Chart dropdowns show raw codes ("15", "20") instead of names. Y-axis has no unit label. Data records table only translates CDEC parameter codes.
Output: Unified parameter label system, live data from all non-CIMIS adapters, human-readable labels across all monitoring pages.
</objective>

<execution_context>
~/.claude/get-shit-done/workflows/execute-phase.md
./summary.md
~/.claude/get-shit-done/references/checkpoints.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/ISSUES.md

# Prior phase (direct dependency):
@.planning/phases/26-geospatial-polish-monitoring-overhaul/26-02-SUMMARY.md

# Key source files:
@datasync/adapters/__init__.py
@datasync/adapters/base.py
@datasync/adapters/cdec.py
@datasync/adapters/usgs.py
@datasync/adapters/dwr_wdl.py
@datasync/adapters/dwr_sgma.py
@datasync/adapters/cimis.py
@datasync/views.py
@datasync/models.py
@templates/datasync/station_detail.html
@templates/datasync/station_list.html
@templates/datasync/partials/_station_list_results.html
@templates/datasync/partials/_monitoring_content.html

**Tech stack available:** Chart.js v4 CDN, Django 5.x + GeoDjango, HTMX, MapLibre GL JS
**Established patterns:** CDEC live sync pipeline (fetch->parse->validate->stage->publish), PARAMETER_MAP dicts per adapter, station_chart_data JSON endpoint
**Constraining decisions:**
- [Phase 26-02]: DATASYNC_MOCK_MODE default=False (live API calls are the norm)
- [Phase 26-02]: Boundary filter removed from station list (watershed stations outside basin polygon must be visible)
- [Phase 26-02]: Chart defaults to "All" date range for sparse historical data
- [Phase 26-02]: CDEC date format fallback already in base adapter staging

**Issues being addressed:** ISS-006, ISS-007 (optional), ISS-008, ISS-009, ISS-010
</context>

<tasks>

<task type="auto">
  <name>Task 1: Create unified parameter label registry</name>
  <files>datasync/adapters/registry.py, datasync/adapters/__init__.py</files>
  <action>
Create `datasync/adapters/registry.py` with a function `get_all_parameter_maps()` that merges PARAMETER_MAP dicts from all adapters into a single dict keyed by `(source_code, parameter_code)` returning `{"name": str, "unit": str}`. Also provide `get_parameter_label(source_code, parameter_code)` that returns "Name (unit)" or falls back to the raw code.

Each adapter already has a PARAMETER_MAP at module level (CDEC: "15"->Reservoir Storage, USGS: "00060"->Discharge, etc.). The registry imports these and builds a merged lookup. This avoids duplicating label dicts in views.

In `__init__.py`, add `from datasync.adapters.registry import get_parameter_label, get_all_parameter_maps` to the public API.

Do NOT duplicate the PARAMETER_MAP contents. Import them from each adapter module.
  </action>
  <verify>python -c "from datasync.adapters.registry import get_parameter_label; print(get_parameter_label('cdec', '15')); print(get_parameter_label('usgs', '00060')); print(get_parameter_label('dwr_wdl', 'gw_level'))"</verify>
  <done>get_parameter_label returns "Reservoir Storage (AF)" for cdec/15, "Discharge (cfs)" for usgs/00060, "Groundwater Level (ft bgs)" for dwr_wdl/gw_level. Unknown codes fall back to raw code string.</done>
</task>

<task type="auto">
  <name>Task 2: Fix chart endpoint and template labels (ISS-008 + ISS-009)</name>
  <files>datasync/views.py, templates/datasync/station_detail.html</files>
  <action>
**views.py — station_chart_data():**
1. Remove the hardcoded PARAM_NAMES dict (lines ~436-441). Replace with: import get_parameter_label from the registry. For each parameter code, look up by `station.data_source.code` to get the correct source-specific label.
2. The `parameters_meta` list should use registry labels for name/unit/label fields.
3. The `dataset_label` should use registry label instead of PARAM_NAMES.get().

**views.py — station_detail():**
1. Replace `from datasync.adapters.cdec import PARAMETER_MAP as CDEC_PARAMETER_MAP` with `from datasync.adapters.registry import get_parameter_label`.
2. Replace the `param_display_map` dict (line ~166) with: for each record, set `rec.param_display = get_parameter_label(station.data_source.code, rec.parameter_code)`.

**station_detail.html — Parameter pills (lines ~84-89):**
Replace `{{ param }}` with a label lookup. Two options:
- Option A (simpler): Pass a `parameter_labels` dict from the view context, keyed by raw code.
- Option B: Use the registry in a template tag.
Choose Option A. In station_detail view, build `parameter_labels = {code: get_parameter_label(station.data_source.code, code) for code in (station.parameters or [])}` and pass in context. Template renders `{{ parameter_labels|dict_lookup:param }}` or use a simple for loop over the dict items.

Actually, simplest approach: pass an `enriched_parameters` list of dicts `[{"code": "15", "label": "Reservoir Storage (AF)"}, ...]` in context. Template iterates over that instead of station.parameters.

**station_detail.html — Chart title (line ~172):**
Change the static "Telemetry" h2 to have an id: `<h2 id="chart-title" class="section-header-flush">Telemetry</h2>`. In the JS loadChart callback, after loading data, update the title: `document.getElementById('chart-title').textContent = data.datasets[0] ? data.datasets[0].label : 'Telemetry';`

**station_detail.html — Chart Y-axis title:**
In the Chart.js config (line ~286), add to the y scale:
```
title: { display: true, text: '', color: '#8899aa', font: { size: 12, family: "'Public Sans', sans-serif" } }
```
In the JS loadChart callback, update y-axis title from API response:
```
var unit = data.parameters.find(function(p) { return p.code === data.selected_parameter; });
chart.options.scales.y.title.text = unit ? unit.unit : '';
chart.update();
```

**station_detail.html — Dropdown initial labels:**
Replace the template-rendered dropdown (lines ~174-179) with options from enriched_parameters so they show human labels on first render, not just after the JS fetch fires. The JS still updates them on data load as a fallback.
  </action>
  <verify>SSH to Butler, rebuild containers, navigate to a CDEC station detail page. Verify: dropdown shows "Reservoir Storage (AF)" not "15", Y-axis has unit label "AF", chart card title updates to match selected parameter, parameter pills show human labels.</verify>
  <done>Chart dropdown shows human-readable parameter names with units. Y-axis displays unit label. Chart title updates dynamically. Parameter pills in station info show names not codes. All verified for CDEC stations (the only source with published data at this point).</done>
</task>

<task type="auto">
  <name>Task 3: Audit monitoring pages for units (ISS-010)</name>
  <files>templates/datasync/station_detail.html, templates/datasync/station_list.html, templates/datasync/partials/_station_list_results.html, templates/datasync/partials/_monitoring_content.html, datasync/views.py</files>
  <action>
Sweep all monitoring templates for places where values appear without units or labels appear as raw codes:

1. **station_detail.html — Recent Data Records table (line ~218):** `record.param_display` already works from Task 2 fix. Verify the unit column also shows. Currently it does show `record.unit` in a small span. Confirm this works for all sources, not just CDEC.

2. **station_detail.html — Chart tooltips:** Chart.js tooltips currently show bare numbers. Add a tooltip callback that appends the unit:
```
tooltip: {
  callbacks: {
    label: function(context) {
      var unit = chart.options.scales.y.title.text || '';
      return context.parsed.y.toLocaleString() + (unit ? ' ' + unit : '');
    }
  }
}
```

3. **station_list_results.html — Sparkline tooltips:** The sparkline SVG has no tooltip. Add a title element or data attribute showing the latest value with unit. If sparkline has data, show "Latest: {value} {unit}" on hover. The unit needs to come from the enriched station data. In station_list view, when building enriched_stations, add the latest record's unit from DataRecordStaging.

4. **monitoring_content.html — Station cards:** Check if station cards show parameter names or raw codes. If raw codes appear, use the registry to translate.

5. **station_list.html — Station list table:** If parameter codes appear in the table, translate them.

Do NOT add units to places where they don't belong (station names, source codes, etc.). Focus on numeric values and parameter identifiers.
  </action>
  <verify>Navigate to station list page. Verify sparklines have hover tooltip with value and unit. Navigate to station detail. Verify chart tooltips show "176,000 AF" not "176000". Verify recent records table shows parameter names with units.</verify>
  <done>Every numeric value across monitoring pages has a unit. Every parameter code displays as a human-readable name. Chart tooltips include units. Sparklines have hover context.</done>
</task>

<task type="auto">
  <name>Task 4: Deploy and run live sync for remaining adapters (ISS-006)</name>
  <files>datasync/management/commands/sync_source.py</files>
  <action>
SSH to Butler (192.168.0.114), pull latest code, rebuild containers, then run live sync for each remaining adapter.

**Pre-flight:** Check which stations exist for each source:
```bash
docker compose exec web python manage.py shell -c "
from datasync.models import MonitoredStation
for code in ['usgs', 'dwr_wdl', 'dwr_sgma']:
    qs = MonitoredStation.objects.filter(data_source__code=code, is_active=True)
    print(f'{code}: {qs.count()} active stations')
    for s in qs: print(f'  {s.external_station_id}: {s.station_name}')
"
```

If zero active stations exist for a source, the discover_stations command from Phase 18 may not have run for that source, or stations were deleted in the Phase 26-02 cleanup. In that case, check if the Kaweah-area stations for that source were preserved:
- USGS: 11210100 (Kaweah R nr Three Rivers), 11208730 (Kaweah R below Terminus Dam)
- DWR WDL: KAW-GWL-01, KAW-GWL-02 (Kaweah groundwater monitoring wells)
- DWR SGMA: TUL-001 (Tulare Lake Subbasin SGMA site)

If stations are missing, recreate them manually via Django shell using the station_add pattern (data_source FK + external_station_id + station_name + Point location). Get coordinates from the ISSUES.md or discover_stations output.

**Sync each source:**
```bash
docker compose exec web python manage.py sync_source usgs --start 2025-01-01
docker compose exec web python manage.py sync_source dwr_wdl --start 2025-01-01
docker compose exec web python manage.py sync_source dwr_sgma --start 2025-01-01
```

**Debug any failures:** Common issues from Phase 26-02 CDEC experience:
- API field names may differ from adapter expectations (check parse method)
- Date formats may not be ISO-compatible (base adapter has fallback, but new formats may appear)
- API may return different response structure than adapter expects
- Sentinel values (-999, -9999) need to be filtered in validate

If an adapter fails, fix the parse/validate methods in the adapter file, commit, rebuild, re-run. Do NOT leave broken adapters. Each must either succeed or be documented as an API issue (e.g., endpoint down, auth required).

**After sync, verify data landed:**
```bash
docker compose exec web python manage.py shell -c "
from datasync.models import DataRecordStaging
for code in ['usgs', 'dwr_wdl', 'dwr_sgma']:
    count = DataRecordStaging.objects.filter(station__data_source__code=code, status='published').count()
    print(f'{code}: {count} published records')
"
```
  </action>
  <verify>Each adapter (usgs, dwr_wdl, dwr_sgma) has published records in DataRecordStaging. sync_source logs show "success" status. Station last_data_at timestamps are updated.</verify>
  <done>USGS, DWR WDL, and DWR SGMA adapters have run live against real APIs. Published records exist for each. Any parsing bugs fixed and committed. Sync logs show success or documented API limitations.</done>
</task>

<task type="checkpoint:human-verify" gate="blocking">
  <what-built>Unified parameter labels across all monitoring pages, Y-axis units on charts, dynamic chart titles, live data from USGS/DWR WDL/DWR SGMA adapters alongside existing CDEC data.</what-built>
  <auto-verify>
    1. SSH to Butler, docker compose up --build -d, verify containers healthy
    2. Run sync_source for each adapter, capture record counts
    3. curl station_chart_data endpoint, verify parameters_meta has human-readable labels
    4. curl station list page, verify 200 response
    5. Check DataRecordStaging counts per source
  </auto-verify>
  <human-verify>
    1. Open station detail for TRM (CDEC) — dropdown shows "Reservoir Storage (AF)", Y-axis labeled "AF", chart title shows parameter name
    2. Open station detail for a USGS station — dropdown shows "Discharge (cfs)" or "Gage Height (ft)", chart renders with data
    3. Open station list — sparklines have hover tooltips with units
    4. Check Recent Data Records table on any station — Parameter column shows names not codes, Value column shows units
  </human-verify>
  <resume-signal>Type "approved" to continue, or describe issues to fix</resume-signal>
</task>

<task type="checkpoint:human-action" gate="optional">
  <action>Register for CIMIS API key (ISS-007)</action>
  <instructions>
    The CIMIS (California Irrigation Management Information System) adapter needs a free API key to fetch evapotranspiration and precipitation data.

    1. Visit https://et.water.ca.gov/Home/Register
    2. Register with your email (free, no approval wait)
    3. Copy the appKey from the confirmation email or account page
    4. Provide the key here — I'll add it to Butler's docker-compose environment and run the CIMIS sync

    If you want to skip this for now, type "skip" — the platform works fine without CIMIS. ET data comes from OpenET instead.
  </instructions>
  <verification>After key is provided: add CIMIS_API_KEY to docker-compose.yml environment, rebuild, run sync_source cimis, verify published records.</verification>
  <resume-signal>Paste the CIMIS API key, or type "skip" to defer</resume-signal>
</task>

</tasks>

<time_estimate>
**Shape:** 6 tasks (2 light, 2 medium, 2 checkpoints), main-context-only, standard.
**Historical matches:** No local telemetry. GSD global baseline: n=1888, median 21 min/session.
**Wall-clock typical (p25-p75):** 15-30 min. **p90:** 45 min.
**Session budget:** Fits single session comfortably.
**Flags:** Live API calls in Task 4 may surface parsing bugs (Phase 26-02 had 8 bug fixes from the same pattern). Budget extra time for adapter debugging.
</time_estimate>

<verification>
Before declaring phase complete:
- [ ] `get_parameter_label('cdec', '15')` returns "Reservoir Storage (AF)"
- [ ] `get_parameter_label('usgs', '00060')` returns "Discharge (cfs)"
- [ ] Station detail chart dropdown shows human names, not raw codes
- [ ] Chart Y-axis has unit label
- [ ] Chart title updates to match selected parameter
- [ ] Recent Data Records table shows parameter names for all sources
- [ ] Chart tooltips include units
- [ ] USGS sync_source completed with published records
- [ ] DWR WDL sync_source completed with published records (or documented API limitation)
- [ ] DWR SGMA sync_source completed with published records (or documented API limitation)
- [ ] All changes committed and pushed
</verification>

<success_criteria>

- All 5 issues addressed (ISS-006 done, ISS-007 optional, ISS-008/009/010 done)
- Every parameter code displays as human-readable label across all monitoring pages
- Every numeric value has a unit
- At least 2 of 3 non-CDEC adapters have live published data
- No regressions to existing CDEC station display
- All verification checks pass
</success_criteria>

<output>
After completion, create `.planning/phases/26.1-monitoring-completion/26.1-01-SUMMARY.md`:

# Phase 26.1 Plan 01: Monitoring Completion Summary

**[Substantive one-liner]**

## Accomplishments
## Files Created/Modified
## Decisions Made
## Issues Encountered
## Next Step
</output>
