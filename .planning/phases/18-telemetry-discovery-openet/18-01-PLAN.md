---
phase: 18-telemetry-discovery-openet
plan: 01
type: execute
---

<objective>
Wire monitoring station auto-discovery into the auto_populate pipeline and build a PostGIS-cached OpenET adapter that stays within the 100-400 queries/month API budget.

Purpose: Stations step completes the auto_populate command (basins + parcels + flowlines + stations), so Phase 19's setup wizard can run a single command to fully populate a new district. OpenET caching prevents budget blowouts by storing ET results locally and re-querying only when data is stale.

Output: `stations` step in auto_populate, OpenETCache model with cache-aware adapter, USGS groundwater well discovery, and tests covering all new code paths.
</objective>

<execution_context>
~/.claude/get-shit-done/workflows/execute-phase.md
./summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md

# Auto-selected based on dependency graph:
@.planning/phases/05-external-data-aggregator/05-01-SUMMARY.md
@.planning/phases/05-external-data-aggregator/05-02-SUMMARY.md
@.planning/phases/17-static-gis-auto-populate/17-01-SUMMARY.md
@.planning/phases/17-static-gis-auto-populate/17-03-SUMMARY.md

# Key files:
@datasync/adapters/base.py
@datasync/adapters/__init__.py
@datasync/adapters/openet.py
@datasync/adapters/usgs.py
@datasync/adapters/cdec.py
@datasync/adapters/cimis.py
@datasync/models.py
@datasync/management/commands/discover_stations.py
@geography/management/commands/auto_populate.py
@geography/services/arcgis.py
@tests/test_auto_populate.py

**Tech stack available:** Django 5, PostGIS 3.4, HTMX, MapLibre, requests, pytest, factory_boy
**Established patterns:**
  - Adapter registry: ADAPTER_REGISTRY dict + register_adapter() + get_adapter()
  - BaseAdapter: abstract fetch/parse/validate/discover_stations, concrete sync/stage/publish
  - auto_populate step registry: OrderedDict, --steps filter, --dry-run, idempotent bulk_create
  - Mock mode via DATASYNC_MOCK_MODE setting
  - discover_stations creates inactive MonitoredStation records for user curation

**Constraining decisions:**
  - Phase 5: OpenET uses station.location point geometry for initial sync (polygon support deferred to this phase)
  - Phase 5: Mock mode controlled by DATASYNC_MOCK_MODE setting OR DataSource.is_active=False
  - Phase 17: ArcGIS client in geography/services/ (one-time setup vs. recurring sync)
  - Phase 17: auto_populate step registry is OrderedDict for deterministic order

**Research topics (Level 1, verify during execution):**
  - OpenET polygon query endpoint format (likely POST to /raster/timeseries/polygon instead of /point)
  - USGS OGC API at api.waterdata.usgs.gov (optional, legacy NWIS still works)
  - CIMIS AppKey env var (CIMIS_API_KEY, already wired in adapter)
</context>

<tasks>

<task type="auto">
  <name>Task 1: Add stations step to auto_populate command</name>
  <files>geography/management/commands/auto_populate.py</files>
  <action>
Add a `stations` step to the auto_populate OrderedDict, after flowlines. The step:

1. Iterates CDEC, USGS, CIMIS data sources (look them up via DataSource.objects.filter(code__in=["cdec", "usgs", "cimis"]))
2. For each source, calls get_adapter(code).discover_stations(boundary.geometry)
3. Creates MonitoredStation records (inactive by default) using get_or_create on (data_source, external_station_id)
4. Respects --dry-run (report counts without creating)
5. Logs per-source discovery counts

Import MonitoredStation and DataSource from datasync.models, get_adapter from datasync.adapters.
Handle missing DataSource records gracefully (warn and skip, don't crash).

The step should catch exceptions per-source so one failing API doesn't block the others. CDEC has no auth (should always work), CIMIS needs CIMIS_API_KEY env var (warn if missing), USGS has no auth.

In mock mode (DATASYNC_MOCK_MODE=True), load station lists from fixture files instead of calling APIs. The fixture files at datasync/fixtures/{source}.json have a "stations" key at the top level.
  </action>
  <verify>python manage.py auto_populate --boundary "Kaweah Subbasin" --steps stations --dry-run (syntax check only, won't hit APIs)</verify>
  <done>auto_populate --steps stations discovers CDEC/USGS/CIMIS stations and creates inactive MonitoredStation records. Idempotent on re-run.</done>
</task>

<task type="auto">
  <name>Task 2: Create OpenETCache model and migration</name>
  <files>datasync/models.py, datasync/migrations/0002_openetcache.py</files>
  <action>
Add OpenETCache model to datasync/models.py:

```
class OpenETCache(models.Model):
    parcel = models.ForeignKey('parcels.Parcel', null=True, blank=True, on_delete=models.CASCADE)
    geometry = models.MultiPolygonField(srid=4326, help_text="Queried geometry")
    start_date = models.DateField()
    end_date = models.DateField()
    variable = models.CharField(max_length=20, default="ET")
    model_name = models.CharField(max_length=50, default="Ensemble")
    et_data = models.JSONField(help_text="Monthly ET values from API response")
    queried_at = models.DateTimeField(auto_now_add=True)

    class Meta:
        indexes = [
            models.Index(fields=["parcel", "start_date", "end_date"]),
            models.Index(fields=["queried_at"]),
        ]

    def is_stale(self, max_age_days=None):
        from django.conf import settings
        from django.utils import timezone
        max_days = max_age_days or getattr(settings, 'OPENET_CACHE_DAYS', 30)
        return (timezone.now() - self.queried_at).days > max_days

    @classmethod
    def monthly_query_count(cls):
        from django.utils import timezone
        month_start = timezone.now().replace(day=1, hour=0, minute=0, second=0, microsecond=0)
        return cls.objects.filter(queried_at__gte=month_start).count()

    @classmethod
    def check_budget(cls, budget=None):
        from django.conf import settings
        limit = budget or getattr(settings, 'OPENET_MONTHLY_BUDGET', 400)
        used = cls.monthly_query_count()
        return used < limit, used, limit
```

Add settings to config/settings/base.py:
- OPENET_CACHE_DAYS = int(os.environ.get("OPENET_CACHE_DAYS", "30"))
- OPENET_MONTHLY_BUDGET = int(os.environ.get("OPENET_MONTHLY_BUDGET", "400"))

Run makemigrations to create the migration. Do NOT hand-write the migration file.
  </action>
  <verify>python manage.py makemigrations datasync --check (migration exists and is up to date)</verify>
  <done>OpenETCache model exists with parcel FK, geometry, date range, et_data JSON, staleness check, and monthly budget counter. Settings wired.</done>
</task>

<task type="auto">
  <name>Task 3: Upgrade OpenET adapter for polygon queries and caching</name>
  <files>datasync/adapters/openet.py</files>
  <action>
Modify the OpenET adapter to support polygon-based queries and cache-aware syncing:

1. Add POLYGON_URL constant: "https://openet-api.org/raster/timeseries/polygon"

2. Add `fetch_polygon(self, geometry, start_date, end_date)` method:
   - Accepts a GEOSGeometry (Polygon or MultiPolygon)
   - Converts geometry to GeoJSON coordinate list for OpenET API
   - POSTs to POLYGON_URL with geometry as GeoJSON coordinates
   - Same 3-stage submit/poll/retrieve pattern as fetch()
   - Falls back to centroid point query if polygon endpoint returns 4xx

3. Add `sync_with_cache(self, parcel, start_date, end_date)` method:
   - Checks OpenETCache for existing non-stale entry matching parcel + date range
   - If cache hit: return cached et_data, log "cache hit"
   - If cache miss or stale: check budget (OpenETCache.check_budget())
     - If over budget: log warning, return None (do NOT query API)
     - If under budget: call fetch_polygon() with parcel.geometry
     - Parse and validate results
     - Store in OpenETCache (parcel FK, geometry, dates, et_data, variable, model)
     - Return parsed results
   - Cache lookup uses parcel FK and overlapping date range (not exact match)

4. Add `sync_parcel_et(self, parcels, start_date, end_date)` management-command-friendly method:
   - Iterates parcels, calls sync_with_cache for each
   - Rate-limited (uses existing rate_limit_seconds)
   - Returns summary dict: {cached: N, fetched: N, budget_blocked: N, failed: N}

Keep the existing point-based fetch() method unchanged for backward compatibility. The polygon path is additive.

Do NOT import or use the Django ORM at module level in openet.py (adapter files are imported early). Use lazy imports inside methods.
  </action>
  <verify>python -c "from datasync.adapters.openet import OpenETAdapter; a = OpenETAdapter(); print('polygon URL:', a.__class__.__dict__.get('fetch_polygon') is not None)"</verify>
  <done>OpenET adapter has fetch_polygon(), sync_with_cache(), sync_parcel_et(). Cache hits skip API calls. Budget exceeded blocks queries with warning. Point-based fetch() unchanged.</done>
</task>

<task type="auto">
  <name>Task 4: Enhance USGS discover_stations for groundwater wells</name>
  <files>datasync/adapters/usgs.py</files>
  <action>
Expand the USGS adapter to discover groundwater monitoring wells in addition to stream sites:

1. Add groundwater parameter codes to PARAMETER_MAP:
   - "72019": {"name": "Depth to Water Level", "unit": "ft below surface"}
   - "72020": {"name": "Water Level Elevation", "unit": "ft NAVD88"}
   - "62610": {"name": "Groundwater Level", "unit": "ft below datum"}

2. Modify discover_stations to query multiple site types:
   - Stream sites: siteType=ST (existing)
   - Groundwater wells: siteType=GW
   - Springs: siteType=SP

   Make two API calls (one for ST, one for GW,SP) and merge results. Each station gets the parameters appropriate to its site type:
   - ST sites get flow/stage/temperature parameters (existing)
   - GW/SP sites get groundwater level parameters (new)
   
   Tag stations with their site type in the returned dict: add "site_type" key ("ST", "GW", "SP").

3. Add a `--site-types` concept to the station dict's parameters list so the discover_stations management command can log what type each station is.

Keep the existing fetch/parse/validate methods unchanged; they already handle the NWIS response format regardless of parameter type.
  </action>
  <verify>python -c "from datasync.adapters.usgs import USGSAdapter, PARAMETER_MAP; print(f'{len(PARAMETER_MAP)} params'); assert '72019' in PARAMETER_MAP"</verify>
  <done>USGS discover_stations finds stream sites, groundwater wells, and springs. GW parameter codes added. Existing fetch/parse/validate unchanged.</done>
</task>

<task type="auto">
  <name>Task 5: Tests for station discovery step and OpenET cache</name>
  <files>tests/test_auto_populate.py, tests/test_openet_cache.py</files>
  <action>
Add tests covering the new functionality:

**In tests/test_auto_populate.py — add TestStationsStep class:**
1. test_stations_step_creates_inactive_stations — mock all 3 adapters' discover_stations to return station lists, verify MonitoredStation records created with is_active=False
2. test_stations_step_idempotent — run twice, verify no duplicates
3. test_stations_step_handles_missing_datasource — remove a DataSource, verify step warns but doesn't crash
4. test_stations_step_dry_run — verify --dry-run creates zero records

**In tests/test_openet_cache.py — new file:**
1. test_cache_creation — create OpenETCache entry, verify fields
2. test_cache_staleness — create entry, check is_stale() returns False for fresh, True for old
3. test_monthly_query_count — create entries, verify count
4. test_budget_check — verify check_budget() returns (True, used, limit) when under budget, (False, used, limit) when over
5. test_sync_with_cache_hit — mock a cached entry, verify no API call made
6. test_sync_with_cache_miss — mock no cached entry + mock API response, verify cache entry created
7. test_budget_blocks_query — set budget to 0, verify sync_with_cache returns None without API call

Use pytest with Django test utilities. Mock external API calls with unittest.mock.patch on the adapter's _request method. Use factory_boy if ParcelFactory exists; otherwise create test parcels inline.

Import patterns: follow existing test_auto_populate.py conventions (pytest.mark.django_db, mock.patch).
  </action>
  <verify>cd /Users/slate/GitHub/openh2o && python -m pytest tests/test_openet_cache.py tests/test_auto_populate.py -v --tb=short 2>&1 | tail -20</verify>
  <done>11+ new tests pass: 4 for stations step, 7 for OpenET cache. All existing tests still pass.</done>
</task>

</tasks>

<time_estimate>
**Shape:** 5 tasks (3 light, 1 medium, 1 light-medium), main-context-only, standard.
**Token budget:**
  - Task 1 (Light): ~15K orchestrator tokens
  - Task 2 (Light): ~15K orchestrator tokens
  - Task 3 (Medium): ~30K orchestrator tokens
  - Task 4 (Light): ~10K orchestrator tokens
  - Task 5 (Light): ~15K orchestrator tokens
  - Total estimated: ~85K / 150K target (57% utilization)
**Historical matches:** No project-specific telemetry. GSD global: Phase 18 n=17, median 25m, p90 298m. OpenH2O project velocity: 5-16 min per plan.
**Wall-clock estimate:** 15-25 min (based on project velocity, not global p90).
**Session budget:** Fits single session.
**Flags:** None.
</time_estimate>

<verification>
Before declaring phase complete:
- [ ] `python manage.py auto_populate --boundary "Kaweah Subbasin" --steps stations --dry-run` runs without error
- [ ] `python manage.py makemigrations datasync --check` confirms migrations up to date
- [ ] `python -m pytest tests/test_auto_populate.py tests/test_openet_cache.py -v` — all tests pass
- [ ] `python -m pytest tests/ -v` — no regressions in existing test suite
- [ ] OpenETCache.check_budget() returns correct tuple
- [ ] auto_populate --help shows stations in available steps
</verification>

<success_criteria>

- All tasks completed
- All verification checks pass
- auto_populate has 4 working steps: basins, parcels, flowlines, stations
- OpenET adapter supports polygon queries with PostGIS caching
- USGS discover_stations finds both stream and groundwater sites
- 11+ new tests passing, no regressions
- Phase 18 complete, ready for Phase 19 (Streaming Dashboard & Setup Wizard)
</success_criteria>

<output>
After completion, create `.planning/phases/18-telemetry-discovery-openet/18-01-SUMMARY.md`
</output>
