---
phase: 23-navigation-restructure-naming
plan: 01
type: execute
---

<objective>
Streamline sidebar navigation from 6 groups to 5 with domain-accurate names. Replace the Infrastructure tab with per-page Add/Import buttons. Add area_override flag to Parcel model. Expand all "DWR" acronyms to proper agency names.

Purpose: Make the navigation match how a water district manager thinks about their work. Prior attempt at sidebar reorg (Phase 19.2-02) was reverted because groupings were confusing; this plan uses a carefully specified target structure.
Output: Reorganized sidebar, renamed pages, Add/Import buttons on Water Data pages, area_override model field, DWR acronym cleanup.
</objective>

<execution_context>
~/.claude/get-shit-done/workflows/execute-phase.md
./summary.md
~/.claude/get-shit-done/references/checkpoints.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md

# Auto-selected based on dependency graph:
@.planning/phases/22-engineering-math-validation/22-01-SUMMARY.md
@.planning/phases/22-engineering-math-validation/22-02-SUMMARY.md
@.planning/phases/19.2-visual-overhaul-ux-refinement/19.2-02-SUMMARY.md

# Key files:
@templates/partials/_sidebar.html
@parcels/models.py
@parcels/signals.py
@infrastructure/views.py
@infrastructure/urls.py

**Tech stack available:** Django 5 + HTMX, PostGIS, MapLibre GL JS, Tailwind standalone, VanderDev tokens
**Established patterns:** PostGIS geography cast for area, boundary-scoped querysets, sidebar-link active class via request.path

**Constraining decisions:**
- [Phase 19.2-02]: Sidebar reorg reverted because domain groupings confused user. This phase uses a specific, pre-approved target structure.
- [Phase 22-01]: PostGIS area auto-calc signal already exists in parcels/signals.py. Uses queryset.update() to avoid recursion. area_override flag must integrate with this signal.
- [Phase 22-01]: Dashboard uses parcel count pro-rating (not area-weighted) for allocations.

**Issues being addressed:** None from ISSUES.md. ISSUE-005 (licensing) is out of scope.
</context>

<tasks>

<task type="auto">
  <name>Task 1: Sidebar restructure and link renames</name>
  <files>templates/partials/_sidebar.html</files>
  <action>
Rewrite the sidebar navigation in _sidebar.html to match this target structure:

**Overview** (no section label, same as current):
- Dashboard → Dashboard (no change)
- Map → Map (no change)

**Water Data** section:
- Use Ledger (was "Ledger") → accounting:ledger_list
- Use Areas (was "Parcels") → parcels:list
- Extraction Wells (was "Wells") → wells:list
- Surface Diversions (was "Surface Water") → surface:water_rights_list
- Recharge Areas (was "Recharge") → recharge:list
- Monitoring Stations (was "Stations" in Monitoring section) → datasync:station_list

**Compliance** section:
- Reports → reporting:report_list (no change)
- Water Years (was "Periods" in Administration) → accounting:periods_list

**Administration** section:
- Accounts → accounting:accounts_list (no change)
- Allocations → accounting:allocations_list (no change)
- Site Health (was "Health" in Monitoring) → health:dashboard
- Setup Wizard → setup:wizard (no change)

**Help** section (no change):
- Getting Started
- Glossary
- About

Remove the "Monitoring" section entirely (Stations moved to Water Data, Health moved to Administration).
Remove Infrastructure link from Administration (replaced by per-page buttons in Task 4).

Keep all existing SVG icons with their current links. Update only the link text and section grouping.
Update the active-class path checks where the link text changed (the URL paths stay the same).
  </action>
  <verify>grep -c "sidebar-section-label" templates/partials/_sidebar.html returns 4 (Water Data, Compliance, Administration, Help). grep "Monitoring Stations\|Extraction Wells\|Surface Diversions\|Recharge Areas\|Use Areas\|Use Ledger\|Water Years\|Site Health" templates/partials/_sidebar.html returns 8 matches.</verify>
  <done>Sidebar has 5 groups (Overview unlabeled + 4 labeled). All 8 renames present. No broken URL references. Infrastructure link removed.</done>
</task>

<task type="auto">
  <name>Task 2: Template title, breadcrumb, and description sweep</name>
  <files>
templates/parcels/list.html, templates/parcels/detail.html,
templates/wells/list.html, templates/wells/detail.html,
templates/surface/water_rights_list.html, templates/surface/water_right_detail.html,
templates/recharge/list.html, templates/recharge/site_detail.html,
templates/accounting/ledger_list.html, templates/accounting/ledger_create.html,
templates/accounting/periods_list.html, templates/accounting/period_create.html, templates/accounting/period_detail.html,
templates/datasync/station_list.html, templates/datasync/station_detail.html, templates/datasync/station_add.html, templates/datasync/monitoring_dashboard.html,
templates/health/dashboard.html,
templates/accounting/csv_upload.html,
templates/help/getting_started.html, templates/help/glossary.html
  </files>
  <action>
Update every affected template's block title, page_title, breadcrumb text, and page-description to match the new names:

- Parcels → Use Areas (list, detail pages)
- Wells → Extraction Wells (list, detail pages)
- Surface Water / Water Rights → Surface Diversions (list, detail pages). Keep "Water Right" when referring to the legal document itself, but the section/navigation name is "Surface Diversions"
- Recharge → Recharge Areas (list, detail pages)
- Ledger → Use Ledger (list, create, upload pages)
- Periods → Water Years (list, create, detail pages)
- Stations → Monitoring Stations (list, detail, add, dashboard pages)
- Health → Site Health (dashboard page)

For each template:
1. Update {% block title %} to use new name
2. Update {% block page_title %} to use new name
3. Update breadcrumb text (both breadcrumb-current and breadcrumb links)
4. Update page-description text to reference new names where applicable
5. Update any help_tooltip includes that reference old names

Do NOT change URL patterns, view function names, or Python model names. Only template display text changes.
Do NOT rename the template files themselves (parcels/list.html stays parcels/list.html).
  </action>
  <verify>grep -rn "block title" templates/parcels/list.html templates/wells/list.html templates/accounting/periods_list.html templates/health/dashboard.html should show "Use Areas", "Extraction Wells", "Water Years", "Site Health" respectively.</verify>
  <done>All affected templates show new names in title, breadcrumb, and description. No template references old names in user-visible text (except where "parcel" or "well" is the domain object name, which stays).</done>
</task>

<task type="checkpoint:human-verify" gate="blocking">
  <what-built>Sidebar restructured with 5 groups and 8 renamed links. All page titles, breadcrumbs, and descriptions updated to match.</what-built>
  <auto-verify>
    1. SSH to Butler (ssh butler), cd ~/openh2o, git pull, docker compose up -d --build
    2. docker compose exec web python manage.py collectstatic --noinput
    3. curl -s -o /dev/null -w "%{http_code}" http://localhost:8010/accounting/dashboard/ (expect 200)
    4. curl -s -o /dev/null -w "%{http_code}" http://localhost:8010/parcels/ (expect 200)
    5. curl -s -o /dev/null -w "%{http_code}" http://localhost:8010/wells/ (expect 200)
    6. curl -s -o /dev/null -w "%{http_code}" http://localhost:8010/surface/ (expect 200)
    7. curl -s -o /dev/null -w "%{http_code}" http://localhost:8010/accounting/reporting-periods/ (expect 200)
    8. curl -s -o /dev/null -w "%{http_code}" http://localhost:8010/health/ (expect 200)
    9. Take screenshot of sidebar via chrome-devtools MCP
  </auto-verify>
  <human-verify>
    1. Sidebar groups make sense: Water Data has 6 items, Compliance has 2, Administration has 4, Help has 3
    2. New names read naturally: "Use Areas" instead of "Parcels", "Extraction Wells" instead of "Wells", etc.
    3. No broken links or empty pages
  </human-verify>
  <resume-signal>Type "approved" to continue, or describe issues to fix</resume-signal>
</task>

<task type="auto">
  <name>Task 4: Add/Import buttons on Water Data pages and remove Infrastructure tab</name>
  <files>
templates/parcels/list.html, templates/wells/list.html,
templates/surface/water_rights_list.html, templates/recharge/list.html,
templates/datasync/station_list.html
  </files>
  <action>
Add action buttons to each Water Data list page header area. The Infrastructure app's add/upload views already handle type-adaptive forms with map drawing. Link to those existing views with type pre-selection where possible.

**Use Areas (parcels/list.html):**
- "Add Use Area" button → infrastructure:add (with ?type=parcel or equivalent)
- "Import" button → infrastructure:upload

**Extraction Wells (wells/list.html):**
- "Add Well" button → infrastructure:add (with ?type=well or equivalent)
- "Import" button → infrastructure:upload

**Surface Diversions (surface/water_rights_list.html):**
- "Add Diversion" button → infrastructure:add (with ?type=diversion or equivalent)
- "Import" button → infrastructure:upload

**Recharge Areas (recharge/list.html):**
- "Add Recharge Site" button → infrastructure:add (with ?type=recharge or equivalent)
- "Import" button → infrastructure:upload

**Monitoring Stations (datasync/station_list.html):**
- "Add Station" button → datasync:station_add (already exists)

Check infrastructure/views.py to determine how the type parameter is handled in infrastructure_add. If it uses a query parameter or form field for type selection, pass it via URL. If the type is selected on the form page itself, just link to infrastructure:add without a parameter.

Use existing btn-primary/btn-sm classes. Place buttons in the breadcrumb-bar area alongside the page title, right-aligned.

Note: The Infrastructure list page itself (/infrastructure/) stays as a valid URL for direct access and bookmarks, but its sidebar link was already removed in Task 1.
  </action>
  <verify>grep -c "infrastructure:add\|infrastructure:upload\|station_add" templates/parcels/list.html templates/wells/list.html templates/surface/water_rights_list.html templates/recharge/list.html templates/datasync/station_list.html — each Water Data page should have at least 1 action button.</verify>
  <done>All 5 Water Data list pages have Add buttons. Parcels, Wells, Diversions, and Recharge pages also have Import buttons. Ledger already has Add + Upload CSV buttons (no change needed).</done>
</task>

<task type="auto">
  <name>Task 5: Add area_override flag to Parcel model</name>
  <files>parcels/models.py, parcels/signals.py, parcels/admin.py</files>
  <action>
Add a BooleanField `area_override` to the Parcel model:

```python
area_override = models.BooleanField(
    default=False,
    help_text="When checked, area_acres is manually set and will not be auto-calculated from geometry."
)
```

Create migration: python manage.py makemigrations parcels

Update parcels/signals.py auto_compute_area_acres to check area_override:
- Change condition from `instance.area_acres is None` to `instance.area_acres is None and not instance.area_override`
- Also skip auto-calc if area_override is True even when area_acres is not None (user may have manually typed a value)

The signal should now be: if geometry is not None AND area_override is False AND area_acres is None, then compute.

If area_override is True, never touch area_acres regardless of whether it's null or has a value.

Update admin.py to show area_override in the Parcel admin (if ParcelAdmin exists), or note it for future admin updates.

Do NOT change any forms or detail templates in this task — the field will be visible in admin and can be set programmatically. UI exposure is a Phase 24 concern.
  </action>
  <verify>python manage.py makemigrations --check returns "No changes detected" (migration already created). python manage.py migrate runs cleanly. grep "area_override" parcels/models.py parcels/signals.py confirms both files updated.</verify>
  <done>Parcel model has area_override BooleanField. Signal respects the flag. Migration created and applied. Existing parcels default to area_override=False (auto-calc behavior preserved).</done>
</task>

<task type="auto">
  <name>Task 6: DWR acronym sweep</name>
  <files>
templates/about.html, templates/wells/detail.html, templates/setup/confirm.html,
datasync/adapters/dwr_sgma.py, datasync/adapters/dwr_wdl.py, datasync/adapters/openet.py,
reporting/generators.py,
core/management/commands/seed_kaweah.py, datasync/management/commands/seed_data_sources.py
  </files>
  <action>
Expand all standalone "DWR" references to the correct full agency name. California has two agencies that share the "DWR" acronym:

1. **Department of Water Resources** (resources.ca.gov) — operates CDEC, Bulletin 118 basins, SGMA Portal, Water Data Library
2. **Division of Water Rights** (waterboards.ca.gov) — operates CalWATRS, eWRIMS, water right permits and licensing

Rules:
- First use in each file: expand fully, e.g. "Department of Water Resources (DWR)"
- Subsequent uses in same file: "DWR" is acceptable
- Where context is ambiguous, always use the full name
- In docstrings and code comments: expand on first use
- In user-facing template text: always expand fully (users may not know the abbreviation)
- In seed data (DataSource name/description fields): expand fully since these appear in UI

Specific file guidance:
- dwr_sgma.py, dwr_wdl.py: These are Department of Water Resources services. Expand in module docstrings.
- reporting/generators.py: "California DWR" in unit citation → "California Department of Water Resources"
- openet.py: "CA DWR" in unit citation → "California Department of Water Resources"
- seed_data_sources.py: "DWR Water Data Library" → "Department of Water Resources Water Data Library", "DWR SGMA Portal" → "Department of Water Resources SGMA Portal", "DWR groundwater" → "Department of Water Resources groundwater"
- seed_kaweah.py: "DWR Basin 5-022.11" → "Department of Water Resources Basin 5-022.11" on first use, "DWR" acceptable after
- about.html: "DWR updates" → "Department of Water Resources updates"
- wells/detail.html: "assigned by DWR" → "assigned by the Department of Water Resources"
- setup/confirm.html: "DWR Bulletin 118" → "Department of Water Resources Bulletin 118"

Do NOT rename Python files (dwr_sgma.py stays dwr_sgma.py) or change class/function names.
Do NOT change adapter keys, URL slugs, or database field values.
  </action>
  <verify>grep -rn "\bDWR\b" templates/ --include="*.html" should return 0 standalone unexpanded DWR in user-facing text (code comments and subsequent-use abbreviations are OK). grep -c "Department of Water Resources\|Division of Water Rights" templates/about.html templates/wells/detail.html templates/setup/confirm.html should return at least 1 per file.</verify>
  <done>All user-facing "DWR" expanded to proper agency name. Python docstrings expanded on first use. Seed data descriptions use full names. No DWR/Division confusion.</done>
</task>

</tasks>

<time_estimate>
**Shape:** 6 tasks (5 auto + 1 checkpoint), main-context-only, standard.
**Token budget:**
  - Task 1 (sidebar rewrite): ~25K
  - Task 2 (template sweep): ~25K
  - Task 3 (checkpoint): ~5K
  - Task 4 (Add/Import buttons): ~15K
  - Task 5 (area_override): ~8K
  - Task 6 (DWR sweep): ~12K
  - Total estimated: ~90K / 150K target (60% utilization)
**Historical context:** Phase 23 telemetry: n=14 sessions, total 400min, median 39min, p90 52min. Global /gsd:execute-plan baseline: n=1825, median 21min, p90 120min.
**Wall-clock estimate:** 25-40 min (single plan, mostly template work).
**Session budget:** Fits single session.
**Flags:** None.
</time_estimate>

<verification>
Before declaring phase complete:
- [ ] All sidebar links load without 404
- [ ] All page titles match new names
- [ ] Breadcrumbs show new names
- [ ] Add/Import buttons link to working views
- [ ] area_override migration applied, signal respects flag
- [ ] No standalone "DWR" in user-facing template text
- [ ] All tests pass: docker compose exec web python -m pytest
- [ ] Deployed to Butler and verified on live site
</verification>

<success_criteria>

- Sidebar has 5 groups with 8 renamed links
- All page titles, breadcrumbs, descriptions updated
- Each Water Data list page has Add button (and Import where applicable)
- Infrastructure sidebar link removed
- Parcel model has area_override BooleanField
- PostGIS auto-calc signal respects area_override flag
- All "DWR" in user-facing text expanded to proper agency name
- 186+ tests still passing
</success_criteria>

<output>
After completion, create `.planning/phases/23-navigation-restructure-naming/23-01-SUMMARY.md`
</output>
