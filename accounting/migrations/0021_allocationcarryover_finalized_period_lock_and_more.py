# Generated by Django 5.2.15 on 2026-09-24 03:22

import pgtrigger.compiler
import pgtrigger.migrations
from django.db import migrations


class Migration(migrations.Migration):

    dependencies = [
        ('accounting', '0020_allocationcarryoverevent_allocationplanevent_and_more'),
    ]

    operations = [
        pgtrigger.migrations.AddTrigger(
            model_name='allocationcarryover',
            trigger=pgtrigger.compiler.Trigger(name='finalized_period_lock', sql=pgtrigger.compiler.UpsertTriggerSql(declare='DECLARE locked_name text;', func='IF TG_OP IN (\'UPDATE\', \'DELETE\') THEN SELECT p.name INTO locked_name FROM accounting_reportingperiod p WHERE p.is_finalized AND ((EXTRACT(YEAR FROM p.end_date)::int + CASE WHEN EXTRACT(MONTH FROM p.end_date) >= 10 THEN 1 ELSE 0 END) = OLD."water_year") ORDER BY p.start_date LIMIT 1; IF locked_name IS NOT NULL THEN RAISE EXCEPTION USING ERRCODE = \'OH409\', MESSAGE = locked_name || \' is finalized. An administrator can reopen it on the period page.\', DETAIL = TG_TABLE_NAME; END IF; END IF; IF TG_OP IN (\'INSERT\', \'UPDATE\') THEN SELECT p.name INTO locked_name FROM accounting_reportingperiod p WHERE p.is_finalized AND ((EXTRACT(YEAR FROM p.end_date)::int + CASE WHEN EXTRACT(MONTH FROM p.end_date) >= 10 THEN 1 ELSE 0 END) = NEW."water_year") ORDER BY p.start_date LIMIT 1; IF locked_name IS NOT NULL THEN RAISE EXCEPTION USING ERRCODE = \'OH409\', MESSAGE = locked_name || \' is finalized. An administrator can reopen it on the period page.\', DETAIL = TG_TABLE_NAME; END IF; END IF; IF TG_OP = \'DELETE\' THEN RETURN OLD; END IF; RETURN NEW;', hash='0978664cd391ded5ee5d9563ef701468bc971fe8', operation='INSERT OR UPDATE OR DELETE', pgid='pgtrigger_finalized_period_lock_14178', table='accounting_allocationcarryover', when='BEFORE')),
        ),
        pgtrigger.migrations.AddTrigger(
            model_name='allocationplan',
            trigger=pgtrigger.compiler.Trigger(name='finalized_period_lock', sql=pgtrigger.compiler.UpsertTriggerSql(declare='DECLARE locked_name text;', func='IF TG_OP IN (\'UPDATE\', \'DELETE\') THEN SELECT p.name INTO locked_name FROM accounting_reportingperiod p WHERE p.is_finalized AND (p.id = OLD."reporting_period_id") ORDER BY p.start_date LIMIT 1; IF locked_name IS NOT NULL THEN RAISE EXCEPTION USING ERRCODE = \'OH409\', MESSAGE = locked_name || \' is finalized. An administrator can reopen it on the period page.\', DETAIL = TG_TABLE_NAME; END IF; END IF; IF TG_OP IN (\'INSERT\', \'UPDATE\') THEN SELECT p.name INTO locked_name FROM accounting_reportingperiod p WHERE p.is_finalized AND (p.id = NEW."reporting_period_id") ORDER BY p.start_date LIMIT 1; IF locked_name IS NOT NULL THEN RAISE EXCEPTION USING ERRCODE = \'OH409\', MESSAGE = locked_name || \' is finalized. An administrator can reopen it on the period page.\', DETAIL = TG_TABLE_NAME; END IF; END IF; IF TG_OP = \'DELETE\' THEN RETURN OLD; END IF; RETURN NEW;', hash='adf5cb42e3be029f220fd0256bad8f25ee068c53', operation='INSERT OR UPDATE OR DELETE', pgid='pgtrigger_finalized_period_lock_b959f', table='accounting_allocationplan', when='BEFORE')),
        ),
        pgtrigger.migrations.AddTrigger(
            model_name='calculationrun',
            trigger=pgtrigger.compiler.Trigger(name='finalized_period_lock', sql=pgtrigger.compiler.UpsertTriggerSql(declare='DECLARE locked_name text;', func='IF TG_OP IN (\'UPDATE\', \'DELETE\') THEN SELECT p.name INTO locked_name FROM accounting_reportingperiod p WHERE p.is_finalized AND ((COALESCE(OLD."period_start", CASE WHEN OLD."period" ~ \'^[0-9]{4}-[0-9]{2}$\' THEN to_date(OLD."period" || \'-01\', \'YYYY-MM-DD\') END)) BETWEEN p.start_date AND p.end_date) ORDER BY p.start_date LIMIT 1; IF locked_name IS NOT NULL THEN RAISE EXCEPTION USING ERRCODE = \'OH409\', MESSAGE = locked_name || \' is finalized. An administrator can reopen it on the period page.\', DETAIL = TG_TABLE_NAME; END IF; END IF; IF TG_OP IN (\'INSERT\', \'UPDATE\') THEN SELECT p.name INTO locked_name FROM accounting_reportingperiod p WHERE p.is_finalized AND ((COALESCE(NEW."period_start", CASE WHEN NEW."period" ~ \'^[0-9]{4}-[0-9]{2}$\' THEN to_date(NEW."period" || \'-01\', \'YYYY-MM-DD\') END)) BETWEEN p.start_date AND p.end_date) ORDER BY p.start_date LIMIT 1; IF locked_name IS NOT NULL THEN RAISE EXCEPTION USING ERRCODE = \'OH409\', MESSAGE = locked_name || \' is finalized. An administrator can reopen it on the period page.\', DETAIL = TG_TABLE_NAME; END IF; END IF; IF TG_OP = \'DELETE\' THEN RETURN OLD; END IF; RETURN NEW;', hash='f284bdaf23d2e19ab274255a67ae7beda99ddac0', operation='INSERT OR UPDATE OR DELETE', pgid='pgtrigger_finalized_period_lock_06974', table='accounting_calculationrun', when='BEFORE')),
        ),
        pgtrigger.migrations.AddTrigger(
            model_name='wateraccountparcel',
            trigger=pgtrigger.compiler.Trigger(name='finalized_period_lock', sql=pgtrigger.compiler.UpsertTriggerSql(declare='DECLARE locked_name text;', func='IF TG_OP IN (\'UPDATE\', \'DELETE\') THEN SELECT p.name INTO locked_name FROM accounting_reportingperiod p WHERE p.is_finalized AND (p.id = OLD."reporting_period_id") ORDER BY p.start_date LIMIT 1; IF locked_name IS NOT NULL THEN RAISE EXCEPTION USING ERRCODE = \'OH409\', MESSAGE = locked_name || \' is finalized. An administrator can reopen it on the period page.\', DETAIL = TG_TABLE_NAME; END IF; END IF; IF TG_OP IN (\'INSERT\', \'UPDATE\') THEN SELECT p.name INTO locked_name FROM accounting_reportingperiod p WHERE p.is_finalized AND (p.id = NEW."reporting_period_id") ORDER BY p.start_date LIMIT 1; IF locked_name IS NOT NULL THEN RAISE EXCEPTION USING ERRCODE = \'OH409\', MESSAGE = locked_name || \' is finalized. An administrator can reopen it on the period page.\', DETAIL = TG_TABLE_NAME; END IF; END IF; IF TG_OP = \'DELETE\' THEN RETURN OLD; END IF; RETURN NEW;', hash='7ca9f83a514a3a6587e35a346775c40b1d83b326', operation='INSERT OR UPDATE OR DELETE', pgid='pgtrigger_finalized_period_lock_b2a48', table='accounting_wateraccountparcel', when='BEFORE')),
        ),
        pgtrigger.migrations.AddTrigger(
            model_name='watercredit',
            trigger=pgtrigger.compiler.Trigger(name='finalized_period_lock', sql=pgtrigger.compiler.UpsertTriggerSql(declare='DECLARE locked_name text;', func='IF TG_OP IN (\'UPDATE\', \'DELETE\') THEN SELECT p.name INTO locked_name FROM accounting_reportingperiod p WHERE p.is_finalized AND ((CASE WHEN OLD."origin_period" ~ \'^[0-9]{4}-[0-9]{2}$\' THEN to_date(OLD."origin_period" || \'-01\', \'YYYY-MM-DD\') END) BETWEEN p.start_date AND p.end_date) ORDER BY p.start_date LIMIT 1; IF locked_name IS NOT NULL THEN RAISE EXCEPTION USING ERRCODE = \'OH409\', MESSAGE = locked_name || \' is finalized. An administrator can reopen it on the period page.\', DETAIL = TG_TABLE_NAME; END IF; END IF; IF TG_OP IN (\'INSERT\', \'UPDATE\') THEN SELECT p.name INTO locked_name FROM accounting_reportingperiod p WHERE p.is_finalized AND ((CASE WHEN NEW."origin_period" ~ \'^[0-9]{4}-[0-9]{2}$\' THEN to_date(NEW."origin_period" || \'-01\', \'YYYY-MM-DD\') END) BETWEEN p.start_date AND p.end_date) ORDER BY p.start_date LIMIT 1; IF locked_name IS NOT NULL THEN RAISE EXCEPTION USING ERRCODE = \'OH409\', MESSAGE = locked_name || \' is finalized. An administrator can reopen it on the period page.\', DETAIL = TG_TABLE_NAME; END IF; END IF; IF TG_OP = \'DELETE\' THEN RETURN OLD; END IF; RETURN NEW;', hash='e87681a91f904f12809c24137790e31caa61158e', operation='INSERT OR UPDATE OR DELETE', pgid='pgtrigger_finalized_period_lock_963c6', table='accounting_watercredit', when='BEFORE')),
        ),
        pgtrigger.migrations.AddTrigger(
            model_name='watercreditdraw',
            trigger=pgtrigger.compiler.Trigger(name='finalized_period_lock', sql=pgtrigger.compiler.UpsertTriggerSql(declare='DECLARE locked_name text;', func='IF TG_OP IN (\'UPDATE\', \'DELETE\') THEN SELECT p.name INTO locked_name FROM accounting_reportingperiod p WHERE p.is_finalized AND ((CASE WHEN OLD."draw_period" ~ \'^[0-9]{4}-[0-9]{2}$\' THEN to_date(OLD."draw_period" || \'-01\', \'YYYY-MM-DD\') END) BETWEEN p.start_date AND p.end_date) ORDER BY p.start_date LIMIT 1; IF locked_name IS NOT NULL THEN RAISE EXCEPTION USING ERRCODE = \'OH409\', MESSAGE = locked_name || \' is finalized. An administrator can reopen it on the period page.\', DETAIL = TG_TABLE_NAME; END IF; END IF; IF TG_OP IN (\'INSERT\', \'UPDATE\') THEN SELECT p.name INTO locked_name FROM accounting_reportingperiod p WHERE p.is_finalized AND ((CASE WHEN NEW."draw_period" ~ \'^[0-9]{4}-[0-9]{2}$\' THEN to_date(NEW."draw_period" || \'-01\', \'YYYY-MM-DD\') END) BETWEEN p.start_date AND p.end_date) ORDER BY p.start_date LIMIT 1; IF locked_name IS NOT NULL THEN RAISE EXCEPTION USING ERRCODE = \'OH409\', MESSAGE = locked_name || \' is finalized. An administrator can reopen it on the period page.\', DETAIL = TG_TABLE_NAME; END IF; END IF; IF TG_OP = \'DELETE\' THEN RETURN OLD; END IF; RETURN NEW;', hash='a7d111b39a1998ca0b87f5d50d6a18be137038b2', operation='INSERT OR UPDATE OR DELETE', pgid='pgtrigger_finalized_period_lock_c11eb', table='accounting_watercreditdraw', when='BEFORE')),
        ),
    ]
